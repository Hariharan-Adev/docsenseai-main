# Frontend changes

## 2026-07-22 — Mutually exclusive sidebar chat selection

- Made nullable `activeConversationId` the single source of truth for chat selection: `null` represents New Chat and a conversation ID represents a selected history chat.
- New Chat now clears the selected conversation and shows the blank chat screen; selecting history removes the New Chat highlight and loads that conversation's messages.
- Persisted valid history selection per authenticated user and restore it after refresh. Missing, deleted, or invalid selections fall back to New Chat.
- Limited chat selection highlighting to the chat view so Documents and chat entries cannot appear active together.
- Preserved native button behavior and added `aria-current`/`aria-pressed` selection state for keyboard and assistive-technology users.
- Existing sidebar styling and layout are unchanged.

## 2026-07-23 — Workbook scope and sheet citations

- Files changed: `frontend/src/services/api.ts`, `frontend/src/context/AppContext.tsx`.
- Change summary: chat requests can include a selected document ID; chat sources accept document, worksheet, and row metadata; source cards display sheet and row citations and open the cited uploaded document by ID.
- Reason: workbook answers need visible tab-level evidence and must remain within the selected workbook boundary.
- Test result: `npm run build` passed TypeScript checking and the Vite production build; Vite reported its existing non-fatal large-chunk warning.
## 2026-07-24 — Queued ingestion, versions, restoration, and structured citations

- Switched individual, folder-file, image, and ZIP uploads to the asynchronous `/api` job endpoints with idempotency keys, polling, terminal failure details, automatic failed-job retry, cancellation-aware UI state, and ZIP child-job progress.
- Added document version history, current-version state, new-version upload, and make-current controls to the document preview.
- Added a recently deleted section and restore action to the document library; regular delete now reflects server-side soft deletion.
- Updated source contracts and cards to display structured page, slide, sheet, cell, and row locations.
- Renamed score language to “retrieval similarity” and explicitly states that vector similarity is not factual confidence.
- Preserved the existing visual system and upload concurrency controls.

## 2026-07-27 14:31:12 +05:30 — Canonical asynchronous API compatibility

- Change summary: no visual behavior changed; the existing job-aware client continues to use supported `/api` compatibility aliases while the backend now also exposes canonical unprefixed asynchronous upload, version, and ingestion-job endpoints.
- Files changed: no additional frontend files in this continuation.
- Reason: retain the shipped queued upload/version UX while stabilizing the public API contract.
- Migration/security impact: none in the browser; tenant identity and ACL decisions remain server-resolved.
- Tests: TypeScript and production-build results are recorded in the final verification entry.

## 2026-07-27 14:43:47 +05:30 — Final frontend verification

- Files verified: complete TypeScript source tree and production Vite bundle.
- Reason: ensure backend contract hardening did not regress queued upload, job polling, version history, restoration, or structured citations.
- Test result: `npm run build` passed TypeScript project compilation and the Vite production build; 2,284 modules transformed. The only notice is the existing non-fatal bundle chunk-size warning.
- Migration/security result: no client migration required; organization, ownership, visibility, and ACL values remain server-authoritative.

## 2026-07-30 - Deleted-document re-upload frontend investigation

Investigation only. No frontend fix was implemented.

### Findings

- `frontend/src/services/api.ts:246-277` creates a fresh idempotency key per upload
  action, submits to `/api/documents/upload`, polls the ingestion job, and converts
  a failed job into an `ApiError`.
- The fresh-key behavior is correct and prevents re-selecting a deleted file from
  replaying the old completed job.
- The queue request returns HTTP 202. The actual failure is asynchronous in the
  backend worker, so it is not available in the initial upload response.
- The backend returns only `internal_processing_error` and
  `Document processing failed.` for the confirmed SQLite failure.
- `frontend/src/components/UploadDocumentsModal.tsx:150-190` displays that safe
  generic message, marks the item failed, and offers Retry Failed.
- Retrying starts another upload action. Because the database constraint is
  deterministic and terminal, retry cannot resolve it and may add another failed
  document/version/content row and retained stored file.
- HTTP 409 is treated as skipped/duplicate, but this incident is reported as a
  polled HTTP-422-style `ApiError`, so the UI correctly labels it failed rather
  than duplicate.
- Delete calls `DELETE /documents/{id}` and refreshes the library. The recently
  deleted list exposes restore. No frontend cache or filename check blocks
  re-upload.
- Error parsing for immediate API failures expects a string `detail`; job failures
  use the separate structured job error path and are handled.

### Frontend impact of the recommended backend fix

- No API shape change is required. Upload can remain 202 plus polling.
- On successful backend correction, same-content re-upload should complete and the
  existing UI will show success.
- If a stable lifecycle conflict code is introduced, map it to an actionable,
  non-sensitive message and do not expose content hashes, deleted-document IDs,
  owner existence, SQL text, or storage keys.
- Disable or relabel automatic retry for terminal non-retryable job codes. Retry
  should remain available for explicitly transient dependency failures.
- Add an end-to-end UI test for delete, re-select the same file, polling completion,
  and library refresh.

### Files inspected

- `frontend/src/services/api.ts`
- `frontend/src/components/UploadDocumentsModal.tsx`
- `frontend/src/components/LibraryModal.tsx`
- `frontend/src/components/DocumentDeleteModal.tsx`
- `frontend/src/context/AppContext.tsx`
- `frontend/src/types/index.ts`

## 2026-07-30 11:38:13 +05:30 — Re-upload status and structured errors

- Files modified: `frontend/src/services/api.ts` and
  `frontend/src/components/UploadDocumentsModal.tsx`.
- The API client now parses safe structured `detail` objects and preserves
  `code` and `retryable` on `ApiError`. Polled job failures use the same fields;
  raw database/provider errors are never rendered.
- Completed deleted-content reuse uses the backend result message and
  `content_reused` flag. The upload table labels that result `restored`.
- `DOCUMENT_ALREADY_EXISTS` displays the backend's clear safe message and is not
  offered as a retryable upload. Retry Failed remains available for transient or
  unspecified failures.
- Polling, cancellation, upload concurrency, and the existing visual design are
  unchanged.
- Security: the client receives no normalized hash, deleted-document identity,
  internal path, SQL, stack trace, or vector-provider detail.
- Verification: `npm run build` passed TypeScript compilation and the production
  Vite build with 2,284 modules transformed. The existing non-fatal bundle
  chunk-size warning remains.
- Rollback: revert both frontend files together; no browser data migration or
  persisted-state cleanup is required.
## 2026-07-30 15:30 +05:30 — Grounding-aware citations

- Root cause contribution: `ChatResponse` omitted `grounded`, while
  `AppContext.sendMessage()` always mapped backend sources and attached the
  first citation. This rendered the contradictory pre-fix backend JSON.
- The response type now includes `grounded`; sources are mapped only when it is
  true, adding a client-side defense to the canonical backend invariant.
- Retrieval documents, confidence, and metadata clear when a request begins, so
  an API failure cannot leave the previous response in the retrieval side panel.
  Conversation history remains intact.
- No client-controlled owner or organization field was added.
- `npm run build` passed TypeScript and Vite production compilation with 2,284
  modules. The existing non-fatal large-chunk warning remains.
- Roll back `api.ts` and `AppContext.tsx` together with the backend response
  invariant if the API contract is reverted.

## 2026-07-31 15:49 +05:30 - Conversation ID in chat payload

- Root cause: the frontend kept a stable local chat/conversation ID, but
  `sendChatMessage()` did not send it to `/chat`, so the backend could not
  resolve pronoun/reference follow-ups server-side.
- `frontend/src/services/api.ts` now includes `conversation_id` in the `/chat`
  JSON payload.
- `frontend/src/context/AppContext.tsx` passes the active or newly-created
  `conversationId` on every chat request. New Chat still starts with
  `activeConversationId=null`, so its first message creates a fresh ID and does
  not reuse old follow-up context.
- Verification: `npm run build` passed TypeScript and Vite production
  compilation. The existing non-fatal large-chunk warning remains.

## 2026-08-03 11:17 +05:30 - Aggregate row-range citation display

- Confirmed root cause: the backend returned structured aggregate source ranges
  with both `row_start` and `row_end`, but the frontend displayed only
  `row_start`, making a multi-row aggregate citation appear as a single row.
- Files modified: `frontend/src/context/AppContext.tsx`.
- Implementation summary: chat source mapping now renders `Row: N` when
  `row_start` and `row_end` are equal, and `Rows: N-M` when the backend
  supplies a multi-row range. Page, slide, sheet, and cell display behavior is
  preserved.
- Security considerations: this is display-only and does not add client-side
  trust in source contents, owner data, tenant IDs, or internal retrieval scores.
- Tests run: `npm run build` passed TypeScript and Vite production compilation
  with 2,284 modules transformed. The existing non-fatal bundle chunk-size
  warning remains.
- Remaining limitations: the chat card still shows the first source in the
  message body; the retrieval panel receives all mapped sources.
2026-08-03 12:23:55 +05:30

- Confirmed root cause: backend aggregate citations could include a row range, but the source mapper only rendered a single row label or a compact start/end label without support for multiple contributing ranges.
- Files modified: `frontend/src/context/AppContext.tsx`, `frontend/src/services/api.ts`.
- Implementation summary: source mapping now renders `Row N` for single-row citations, `Rows N-M` for contiguous ranges, and comma-separated ranges when backend citations include multiple `row_ranges`. The API source-location type now allows `row_ranges` arrays.
- Security and tenant-isolation considerations: frontend changes only format backend-provided citation metadata and do not alter authorization, document access, or data fetching.
- Tests run: `npm run build` -> TypeScript build and Vite production build OK; Vite reported only the existing large chunk warning.
- Remaining risks or limitations: page/slide range display remains limited to the existing single start value unless backend supplies richer page/slide range fields in future.
