## 2026-07-23 — Domain-neutral Excel ingestion and analysis

- Files changed: `backend/app/config.py`, `backend/app/database.py`, `backend/app/routes/chat.py`, `backend/app/routes/search.py`, `backend/app/routes/upload.py`, `backend/app/services/rag_service.py`, `backend/app/services/vector_search.py`, `backend/app/services/workbooks.py`, `backend/app/services/workbook_analysis.py`, and `backend/tests/test_workbook_domain_neutral.py`.
- Change summary: added safe all-sheet Excel extraction, structured owner-scoped sheet/row storage, row-level semantic chunks, deterministic cross-sheet calculations and lists, dynamic schema/alias matching with clarification, sheet/document scope, and sheet/row citations.
- Reason: arbitrary business workbooks must be queried across all tabs without employee-specific assumptions or top-K-based calculations.
- Test result: `python -m unittest discover -s tests -v` passed all 51 tests; `npm run build` passed with only Vite's existing non-fatal large-chunk warning.
## 2026-07-24 — Tenant-aware asynchronous RAG architecture

- Added organization-scoped JWT context, `member`/`organization_admin` roles, centralized private/organization/direct-share ACL predicates, safe 404 denials, and document share/visibility APIs.
- Added `202 Accepted` document, version, folder-file, image, and ZIP ingestion endpoints backed by durable idempotent jobs; the standalone worker now claims jobs atomically, recovers stale locks, batches embeddings, retries with exponential backoff and jitter, records safe terminal errors, supports cancellation/retry, and fans ZIP jobs into child document jobs.
- Replaced application-side JSON-vector scanning with a Qdrant provider abstraction, deterministic point IDs, tenant/ACL/current-version/deletion filters, health validation, payload updates for lifecycle changes, and a safe full/tenant reindex command.
- Added format-aware provenance for PDF pages, PowerPoint slides/shapes/notes, Word sections/paragraphs/tables, Excel sheets/rows/columns/cells/tables/formulas/hidden sheets, CSV rows, text lines, and OCR/vision image chunks.
- Added version list/detail/make-current APIs, soft delete/restore, admin-gated hard purge, archive progress results, request/job correlation IDs, structured JSON logs, readiness and metrics endpoints, organization-scoped rate/quota keys, token/cost caps, and audit events for sensitive transitions.
- Preserved the legacy synchronous upload and image interfaces as compatibility endpoints while the React client uses the queued APIs.
- Validation: all 61 Python 3.12 backend tests pass, including the new tenant/job/version/ACL/archive/lifecycle and real Qdrant-filter contracts; `npm run build` passes with only Vite's existing non-fatal large-chunk warning.

## 2026-07-27 14:31:12 +05:30 — Version-aware duplicate, storage, and security hardening

- Change summary: made `/documents/upload` and `/ingestion-jobs/{id}` the canonical `202` asynchronous APIs; enforced same-name versioning and explicit identical-version rules; constrained normalized-content reuse to readable documents within one organization while retaining distinct chunks/vector points; added opaque organization-scoped storage keys, Office archive inspection, parser limits, production Qdrant TLS/auth checks, tenant denial logs, and per-tenant stage/lifecycle metrics.
- Files changed: `backend/app/config.py`, `backend/app/main.py`, `backend/app/routes/ingestion.py`, `backend/app/routes/upload.py`, `backend/app/routes/documents.py`, `backend/app/routes/health.py`, `backend/app/services/document_access.py`, `backend/app/services/ingestion_jobs.py`, `backend/app/services/source_extraction.py`, `backend/app/services/storage.py`, `backend/app/services/vector_store.py`, `backend/app/utils/file_validation.py`, `backend/.env.example`, `backend/README.md`, `docker-compose.yml`, and backend tests.
- Reason: close the remaining production requirements for duplicate/version semantics, storage isolation, bounded parsing, authenticated vector infrastructure, safe job control, and measurable ingestion stages.
- Security impact: prevents cross-organization content reuse and storage traversal, preserves safe 404 IDOR behavior, rejects suspicious Office containers, binds development Qdrant locally, and requires HTTPS plus API authentication in production.
- Migration impact: adds data-preserving job metric columns through migration `006_ingestion_metrics`; startup now rolls back when foreign-key or tenant ownership validation fails.
- Tests: focused schema, full queued lifecycle, and tenant-safe duplicate/version/storage tests pass; complete regression results are recorded in the final verification entry.

## 2026-07-27 14:43:47 +05:30 — Final verification

- Files verified: complete backend application and test suite, canonical OpenAPI route set, frontend TypeScript application, production bundle, and repository diff whitespace.
- Reason: production handoff gate for the multi-tenant document architecture continuation.
- Test result: all 71 Python 3.12 backend tests passed in 399.774 seconds; after adding hard production parser termination, its four security tests plus the focused worker retry regression also passed. Python compilation passed for every changed backend module; OpenAPI contains all canonical upload/version/job routes; `npm run build` passed with 2,284 modules transformed and only Vite's existing non-fatal large-chunk warning; `git diff --check` reported no whitespace errors (only Windows CRLF conversion notices).
- Migration/security result: legacy migration preservation, tenant ACL/IDOR denial, duplicate/version/reuse isolation, retry recovery, Office/ZIP defenses, Qdrant tenant filters, organization storage partitions, canonical safe-not-found job access, and operational metrics are covered by passing tests.

## 2026-07-27 15:20:46 +05:30 — Soft-delete upgrade repair

- Change summary: added lifecycle repair migration `007` so upgraded databases that recorded migration `004` before its cascade-attribution columns existed are repaired from the actual table schema.
- Files changed: `backend/app/database.py`, `backend/tests/test_multitenant_architecture.py`.
- Reason: live soft-delete requests returned HTTP 500 because `document_versions`, `document_contents`, and `chunks` lacked `deleted_with_document` despite the older migration ledger entry.
- Migration impact: additive, idempotent, and data preserving; each missing column is added with a zero default, and the migration self-heals even if its own ledger entry exists but the physical schema is incomplete.
- Security impact: restores transactional soft delete/restore behavior without weakening tenant or ownership checks.
- Tests: the partial-upgrade repair regression and full queued lifecycle/delete/restore integration test both pass.

## 2026-07-30 - Production investigation: deleted documents cannot be re-uploaded

Investigation only. No backend fix was implemented.

### Confirmed root cause

The canonical asynchronous upload accepts the replacement with HTTP 202, but the
worker fails after extraction and embedding and before chunk/vector indexing.

1. `backend/app/routes/documents.py:448-525` soft-deletes the document, versions,
   chunks, and orphaned content. It intentionally retains all rows and stored files.
2. `backend/app/database.py:332-333` has the unconditional unique index
   `idx_document_contents_owner_content_hash` on
   `(owner_id, normalized_content_hash)`. Soft-deleted rows remain covered by it.
3. During re-upload, `backend/app/routes/ingestion.py:143-151` ignores the deleted
   logical document and creates a new document and placeholder content row.
4. The worker's reuse query at
   `backend/app/services/ingestion_jobs.py:617-647` correctly excludes deleted
   content, versions, and documents.
5. The worker assigns the extracted normalized hash to the new placeholder at
   `backend/app/services/ingestion_jobs.py:664-668`. SQLite rejects this update
   because the deleted content row still owns that hash under the unconditional
   unique index.

The exact reproduced exception is:

`sqlite3.IntegrityError: UNIQUE constraint failed: document_contents.owner_id, document_contents.normalized_content_hash`

The failure is content-based, not filename-based. Different bytes that normalize
to the same extracted text fail as well.

### Complete upload lifecycle

1. `queue_document_upload` in `backend/app/routes/ingestion.py:66-278` authenticates
   the user, rate-limits, sanitizes the filename, validates type/signature and size,
   reads the upload, calculates the SHA-256 file hash, and validates collection and
   folder context.
2. A fresh opaque filename and organization-partitioned storage key are created.
   The file is written before the SQL transaction.
3. A `BEGIN IMMEDIATE` transaction resolves an explicit document or an active
   same-owner/same-name document. Deleted documents are excluded.
4. For an active same-name document, an active same-file-hash version is rejected
   with HTTP 409 unless the explicit version endpoint is used.
5. A placeholder `document_contents` row, a new logical `documents` row when
   needed, an immutable `document_versions` row, and an `ingestion_jobs` row are
   committed together. A queueing exception removes the newly stored file.
6. The worker claims the job using compare-and-set state transitions, verifies the
   stored file hash/signature, extracts structured chunks, normalizes text, and
   creates embeddings.
7. In one SQLite transaction the worker either reuses readable active content or
   updates the placeholder content, replaces SQL chunks, and stores workbook
   metadata. The confirmed failure occurs at the placeholder update, before new
   chunks are inserted.
8. A later step stores deterministic point IDs on chunks and upserts Qdrant points.
9. A final SQLite transaction marks the version/job complete and changes
   `documents.current_version_id`. Search cannot use the new version before this.

No exception is silently swallowed. `process_job` logs the traceback at
`backend/app/services/ingestion_jobs.py:850-858`, classifies the SQLite integrity
failure as terminal `internal_processing_error`, and stores only the safe public
message `Document processing failed.` The frontend therefore receives a safe but
non-diagnostic error after polling.

### Complete delete lifecycle

Soft delete (`DELETE /documents/{id}`):

- Requires tenant-scoped manage permission.
- Calls Qdrant first and sets `deleted` and `is_deleted` payload fields to `true`;
  it does not remove vectors.
- In one SQLite `BEGIN IMMEDIATE` transaction, sets deletion attribution on
  `documents`, every active `document_versions` row, and every active `chunks` row;
  cancels queued/retry jobs; soft-deletes content no longer referenced by a live
  version/document; and cancels queued versions.
- Retains physical files, SQL embeddings, vector point IDs, workbook metadata,
  audit events, permissions, and ingestion history.
- Returns `file_deleted: false` and explicitly reports retention.

Restore clears lifecycle fields only for successfully completed versions and their
cascade-deleted chunks/content, then marks Qdrant payloads active.

Hard delete (`DELETE /documents/{id}/hard`) is disabled by default and admin-only.
It physically deletes Qdrant points, chunks, jobs, permissions, versions, the
document, unreferenced content (with cascading workbook metadata), and finally
local stored files.

### Transaction and failure integrity

The SQLite portions of soft delete use a transaction, but the full delete is not
atomic across Qdrant and SQLite:

- Qdrant is updated before SQLite.
- A handled SQLite exception attempts a compensating Qdrant update.
- There is no durable outbox, retry, or reconciliation job for vector lifecycle
  changes.
- A process crash between Qdrant and SQLite can leave an active SQL document hidden
  in Qdrant. A failed compensation can also leave inconsistent state.
- Hard delete has larger partial-failure windows because Qdrant deletion precedes
  SQL deletion and file deletion follows the SQL commit.

Upload is staged rather than globally atomic. SQL chunk persistence commits before
Qdrant upsert so retry can resume. This permits recoverable SQL chunks without
vectors after a transient vector failure and possible vectors for an unactivated
version if final SQL activation fails. The confirmed uniqueness failure is
transactional: the placeholder/hash update rolls back and no chunks or vectors are
created for that failed version.

### Duplicate detection findings

- Transport retry detection: caller-provided idempotency key, scoped by
  organization.
- Same logical document detection: case-insensitive original/display filename,
  scoped to the authenticated organization and owner, active documents only.
- Identical active version detection: raw file SHA-256 within that logical
  document, excluding deleted/cancelled versions.
- Cross-filename content reuse: normalized extracted-text SHA-256, limited to
  completed, active content referenced by an active readable document in the same
  organization. Readability is owner, organization visibility, or organization
  admin. It does not cross organizations.
- The intended active-only reuse predicate is correct. The database uniqueness
  rule is not lifecycle-aware and contradicts it.
- Stored filename is random and is not used for duplicate detection.

### Reproduction results

All tests used isolated temporary databases, the real API/worker state machine,
the real schema, a fake vector provider, deterministic embeddings, valid preflight
signatures, and deterministic extraction. No production rows were modified.

| After successful soft delete | Upload HTTP | Worker result |
| --- | ---: | --- |
| Same filename, same content | 202 | Failed: `internal_processing_error` |
| Same filename, different content | 202 | Completed |
| Different filename, same content | 202 | Failed: `internal_processing_error` |
| Different filename, different content | 202 | Completed |
| Different bytes, same normalized text | 202 | Failed: `internal_processing_error` |
| Same TXT | 202 | Failed |
| Same PDF | 202 | Failed |
| Same DOCX | 202 | Failed |
| Same XLSX | 202 | Failed |
| Same PPTX | 202 | Failed |
| Same PNG/image | 202 | Failed |

The format matrix isolates lifecycle/persistence behavior; parser correctness was
not under test. The failure is format-independent because all formats converge on
the same normalized-hash update.

### Logging findings

- Worker tracebacks contain the exact SQLite exception and job correlation ID.
- Persisted jobs expose only `internal_processing_error` and
  `Document processing failed.`, which is safe but not operationally specific.
- The inspected local database has 8 failed ingestion jobs with that generic code.
  Six have a raw file hash matching a deleted same-owner document; the currently
  non-deleted failed record matches both the name and bytes of a deleted document.
- Historical API logs also contain a separate `deleted_with_document` missing-column
  delete failure. Current schema includes the repaired columns and later logs show
  successful deletes, so it is not the current re-upload cause.
- Vector lifecycle failures are logged only through request exceptions. There is
  no persisted lifecycle-operation state, retry count, or reconciliation result.

### Storage and vector findings

- Storage is local filesystem storage under an opaque organization partition;
  no object-storage implementation is present.
- Soft delete deliberately retains files. In the inspected environment all 31
  version rows had a physical file; this includes all 29 versions belonging to
  deleted documents and all 8 failed versions.
- A worker failure does not remove the accepted upload, so every failed re-upload
  consumes another stored object.
- Regular upload writes directly to its final opaque key. Queue transaction failure
  removes it. ZIP extraction uses a self-cleaning server-controlled temporary
  directory.
- Soft delete does not remove vectors; it marks payloads deleted. Unit/integration
  vector contracts verify deleted points are excluded and can be restored.
- The local Qdrant endpoint was unavailable during the investigation, so actual
  point payload state could not be verified without making an assumption. SQL
  retains vector IDs for 906 deleted chunks.
- There is no vector-deletion retry for soft delete. Hard delete uses physical
  Qdrant deletion with no retry/reconciliation record.

### Additional issues discovered

1. `initialize_database` calls `_create_indexes` before ledgered migrations on
   every startup (`backend/app/database.py:944-953`). `_create_indexes` recreates
   the obsolete unique index `idx_chunks_content_chunk_index` on
   `(content_id, chunk_index)`, while migration 002 replaced it with
   `(content_id, version_id, chunk_index)`. The inspected database contains both.
   After a restart, active cross-filename content reuse can therefore collide when
   creating the new version's chunks. Fresh one-start tests do not detect this.
2. Failed initial uploads leave visible logical documents with a failed version,
   pending placeholder content, and retained files. Repeated retry in the UI cannot
   succeed for this terminal constraint violation and can accumulate records/files
   when the user starts fresh upload actions.
3. Re-uploading a deleted filename receives a suffixed display filename because
   `idx_documents_owner_display_filename` also includes deleted rows and the name
   generator intentionally considers all rows. It does not cause the reported
   failure, but it is a lifecycle/UX side effect.
4. Workbook metadata has no independent soft-delete columns. It remains reachable
   through retained soft-deleted content; this is retained data, not a broken
   foreign-key orphan.

### Recommended production fix (not implemented)

Preferred policy: keep historical soft-deleted content restorable, but make active
content identity lifecycle-aware and tenant-aware.

1. Add an idempotent migration that drops
   `idx_document_contents_owner_content_hash` and creates an active-only partial
   unique index using the chosen ownership policy, preferably
   `(organization_id, owner_id, normalized_content_hash) WHERE deleted_at IS NULL`.
   If content reuse is intended at organization scope rather than owner scope,
   align both the unique key and authorization/reuse query to that explicit policy.
2. In the same migration, permanently remove
   `idx_chunks_content_chunk_index`; stop `_create_indexes` from recreating it; and
   retain only the version-aware uniqueness rule.
3. Keep the worker reuse query active-only. Add explicit handling for the narrow
   uniqueness race: re-read the winning active content under the same tenant/ACL
   policy or return a stable duplicate-conflict code. Do not expose hashes, SQL,
   storage paths, or cross-tenant existence.
4. Add regression tests that initialize the same database twice and cover
   delete/re-upload, normalized-equivalent bytes, active content reuse, different
   users/organizations, restore after replacement upload, concurrent uploads, all
   supported format routes, and hard-delete cleanup.
5. Introduce a durable outbox/reconciliation state for Qdrant lifecycle changes,
   with bounded retries and alerting. Search must continue to enforce SQL
   active/current-version predicates as defense in depth.
6. Add a stable safe public code such as `content_identity_conflict` only if the
   condition remains possible after the migration. Keep detailed SQLite/provider
   text server-side.
7. Plan a cleanup migration/job for pre-existing failed placeholder
   documents/content/files only after establishing retention and audit policy.
   Do not delete historical or customer data automatically.

Risks and compatibility:

- A partial unique index is supported by SQLite and preserves soft-deleted history,
  restore, audit, and search exclusion.
- Restore can conflict if a replacement active content row now owns the same
  identity. The restore endpoint needs a defined conflict policy (reject restore,
  merge references, or retire the replacement); it must not silently reactivate
  two active identities.
- Existing active duplicate rows must be audited before creating the new index.
- Search behavior should be unchanged because only completed active current
  versions are retrieved. Reindexing is not required for the hash-index migration.
- Removing the obsolete chunk index restores the documented per-version chunk and
  deterministic-point design; it may expose previously blocked reuse paths, so
  concurrency and ACL regression tests are mandatory.
- API shape can remain backward compatible: upload still returns 202 and job
  polling remains the completion contract.

### Verification performed

- Isolated delete/re-upload matrix: 11 scenarios completed and reproduced the
  expected content-dependent failure/success split.
- Exact server traceback captured for every same-normalized-content failure.
- Existing focused regressions passed:
  `tests.test_qdrant_vector_store` and
  `MultitenantArchitectureTests.test_duplicate_versioning_reuse_and_storage_are_tenant_safe`
  (2 tests, 26.347 seconds).
- `PRAGMA integrity_check` and `PRAGMA foreign_key_check` passed on the inspected
  database.
- `git diff --check` passed for all three investigation reports.

### Files inspected

- Backend lifecycle: `backend/app/routes/ingestion.py`,
  `backend/app/services/ingestion_jobs.py`, `backend/app/routes/documents.py`,
  `backend/app/services/document_access.py`, `backend/app/worker.py`.
- Persistence/storage/vector: `backend/app/database.py`,
  `backend/app/services/storage.py`, `backend/app/services/vector_store.py`,
  `backend/app/services/vector_search.py`, `backend/app/services/zip_archives.py`.
- Validation/extraction: `backend/app/utils/document_content.py`,
  `backend/app/utils/file_validation.py`,
  `backend/app/services/source_extraction.py`,
  `backend/app/services/document_loader.py`.
- Logging/configuration: `backend/app/utils/observability.py`,
  `backend/app/utils/audit.py`, `backend/app/config.py`, `backend/app/main.py`,
  `backend/README.md`, root `README.md`.
- Tests: `backend/tests/test_multitenant_architecture.py`,
  `backend/tests/test_document_duplicates.py`,
  `backend/tests/test_qdrant_vector_store.py`,
  `backend/tests/test_document_parsers.py`,
  `backend/tests/test_image_upload.py`.
- Runtime evidence: read-only SQLite inspection of `backend/data/rag_new.db`,
  storage existence/count checks under `backend/data/uploads`, and backend API/
  worker logs under `backend/data`. No document text, secret, token, or hash value
  was copied into this report.
- Frontend files are listed in `changes/changes_frontend`.
- `Rag-demo` is a Git submodule and was not treated as the root production source.

## 2026-07-30 11:38:13 +05:30 — Deleted-document re-upload fix

### Summary and root cause addressed

- Files modified: `backend/app/services/ingestion_jobs.py`,
  `backend/app/routes/ingestion.py`, `backend/app/routes/documents.py`, and
  `backend/tests/test_multitenant_architecture.py`.
- Re-upload failed after extraction, before chunk creation and vector indexing,
  because the worker tried to assign a deleted row's normalized hash to a new
  active placeholder while SQLite still enforced unconditional owner/hash
  uniqueness.
- The worker now resolves normalized content identity before embedding generation.
  Active identical content is rejected with `DOCUMENT_ALREADY_EXISTS`; compatible
  deleted content is deterministically selected by lowest content ID and safely
  reused; incompatible historical content follows normal ingestion.

### Implementation details

- Added one owner- and organization-scoped duplicate resolver with explicit
  `active_duplicate`, `active_reuse`, `deleted_reuse`, `deleted_rebuild`, and
  `new` outcomes.
- Deleted chunks are reused only when chunk count, ordered text, source type,
  embedding model, dimension, and stored vector shape are compatible.
- A deleted canonical content row is reactivated in the same `BEGIN IMMEDIATE`
  transaction that links the new version, removes its unused placeholder, and
  creates version-scoped chunks. Historical versions, chunks, files, audit rows,
  and workbook metadata remain intact.
- Reused embeddings receive new deterministic point IDs for the new document and
  version. Qdrant upserts are idempotent. Missing points are rebuilt on retry from
  persisted chunks without extraction or embedding regeneration.
- Missing or incompatible embeddings rebuild only the embedding/vector layer.
  A completed version is not exposed until vector upsert succeeds.
- Successful deleted-content reuse records `document.reupload.reused` and returns
  a safe result message. Active duplicate failures return
  `DOCUMENT_ALREADY_EXISTS`, are terminal, and retire the unused queued
  document/version/content placeholder without deleting history.
- Job reads, retry, and cancellation are strictly owner-scoped. Restore resolves
  identity using the document owner's scope and rejects a conflict instead of
  creating two active canonical rows.
- Failure logs include job, owner, document, version, stage, and safe error class;
  document text, hashes, SQL, paths, tokens, and provider internals are not
  returned to clients.

### Security and transaction considerations

- All content resolution and job operations use server-derived owner and
  organization identifiers and parameterized values. Client hashes are never
  trusted.
- Existing upload validation, parser limits, normalized-text calculation, and
  safety scanning remain in the canonical worker path.
- `BEGIN IMMEDIATE`, the active-only unique index, and explicit integrity-error
  classification provide the race boundary for simultaneous identical uploads.
- SQLite and Qdrant cannot share a transaction; durable processing state plus
  deterministic point IDs makes vector upsert retry-safe.

### Tests and verification

- Added regression coverage for delete/re-upload with the same and different
  names, active duplicates, different-content versioning, cross-user isolation,
  embedding/chunk reuse, missing-embedding rebuild, missing-vector recovery,
  repeated job execution, restart-safe indexes, migration conflicts, search, and
  chat citations.
- Added a TXT/PDF/DOCX/XLSX/PPTX/PNG lifecycle matrix. Existing parser tests
  continue to verify format-specific parsing and source provenance.
- Full backend result:
  `.venv312\Scripts\python.exe -m unittest discover -s tests -v` —
  82 tests passed in 271.224 seconds after the final owner-scope change.
- Search returned only the replacement active document, chat cited that document,
  active duplicate rejection remained terminal, and valid reuse did not call the
  embedding generator.

### Rollback notes and remaining risk

- Application rollback requires rolling back these worker/route changes together
  with the database index migration. Restore the pre-deployment database backup
  before starting the older application because the older startup code recreates
  obsolete indexes.
- Do not automatically delete retained files, historical content, or audit rows.
- Qdrant remains an external system without a transactional outbox. Retry-safe
  upserts cover ingestion failures, but delete/restore reconciliation should
  eventually receive durable operational tracking.

## 2026-07-30 12:13:31 +05:30 — Qdrant mode and configurable retrieval policy

- Files modified: `backend/app/config.py`,
  `backend/app/services/vector_store.py`,
  `backend/app/services/vector_search.py`, `backend/app/services/rag_service.py`,
  `backend/.env.example`, `backend/README.md`, and `backend/tests/test_chat.py`.
- Existing architecture confirmed: `qdrant-client==1.18.0` was already pinned;
  ingestion stores extracted chunks and provenance in SQLite; one Qdrant
  collection stores embeddings and searchable payloads; and vector queries
  enforce organization, owner/visibility, deletion, and current-version filters.
- Added backward-compatible `QDRANT_MODE` selection with `auto`, `local`,
  `remote`, and `memory`. `QDRANT_PATH` is the preferred persistent local path;
  `QDRANT_LOCAL_PATH` remains supported. Production continues to require remote
  Qdrant over HTTPS with an API key.
- Remote collections now create idempotent payload indexes for
  `organization_id`, `owner_id`, `document_id`, `document_version_id`,
  `visibility`, and `is_deleted`, matching the multitenant query predicates.
- Added validated `RAG_RETRIEVAL_LIMIT`, `RAG_FINAL_CONTEXT_LIMIT`, and
  `RAG_MIN_SCORE` settings. Chat retrieves 15 candidates by default, removes
  results below 0.35, and sends at most 5 chunks to the LLM.
- No reranker was added because it is optional and no evaluated reranking model
  or corpus benchmark was supplied. The extension point remains between
  candidate retrieval and final context slicing.
- `EMBEDDING_DIMENSION` remains configuration-driven. Collection startup and
  every upsert reject vectors that do not match it; no dimension was newly
  hard-coded.
- Security: tenant/ACL filtering stays inside the Qdrant query, and the
  application also obtains authorized current version IDs from SQLite before
  vector search.
- Verification: full backend command
  `.venv312\Scripts\python.exe -m unittest discover -s tests -v` passed all
  83 tests in 220.783 seconds. The new test verifies the 15-candidate,
  score-threshold, 5-context contract. Python compilation and
  `git diff --check` passed.
- Rollback: revert the seven files above and remove the new environment values.
  No SQLite or Qdrant data migration is required; payload indexes may remain
  safely because they do not change point data or query semantics.

## 2026-07-30 12:25:40 +05:30 — Chunk-to-Qdrant synchronization contract

- Files modified: `backend/app/database.py`,
  `backend/app/services/vector_store.py`,
  `backend/app/services/vector_search.py`,
  `backend/app/services/ingestion_jobs.py`, `backend/app/reindex.py`,
  `backend/app/routes/upload.py`, `backend/README.md`,
  `backend/tests/test_qdrant_vector_store.py`,
  `backend/tests/test_multitenant_architecture.py`, and
  `backend/tests/test_folder_uploads.py`.
- Integrated the attached Qdrant service requirements into the existing
  `VectorStore`/`QdrantVectorStore` abstraction. A separate global-client
  `qdrant_store.py` was not added because it would duplicate collection
  initialization, bypass production transport checks, and split lifecycle logic.
- Added `content_id` to every Qdrant point payload and its remote payload index.
  Existing structured `source_location` continues to carry page, slide, sheet,
  cell, and row provenance without duplicating format-specific fields.
- Pushed `RAG_MIN_SCORE` into Qdrant's `score_threshold` while retaining the
  application-side threshold as defense against provider implementations that do
  not support server-side filtering.
- Retained deterministic UUIDv5 point IDs instead of generating UUIDv4 during
  every chunk insertion. The ID is stable for organization, version, chunk index,
  and embedding model, is persisted in SQLite, and makes retries overwrite the
  same Qdrant point.
- Canonical ingestion marks chunks `pending` before upsert, `failed` after a
  provider failure, and `completed` with `qdrant_indexed_at` only after confirmed
  upsert. Retry recovery and full reindex update the same synchronization fields.
- Security remains organization- and ACL-filtered in Qdrant, with authorized
  current-version IDs calculated from SQLite before search.
- Verification: full backend suite passed 83 tests in 311.538 seconds. Focused
  tests verified payload `content_id`, server-side threshold exclusion, schema,
  failure status, retry completion timestamps, and restored-document search.
  Python compilation and `git diff --check` passed.
- Rollback: revert the service and ingestion changes together with migration 009.
  Restore the pre-deployment SQLite backup before running older code if strict
  schema rollback is required. Qdrant points remain compatible because point IDs
  and existing payload fields were not changed.

## 2026-07-30 12:52:50 +05:30 — Canonical Qdrant ingestion and grounded retrieval

- Files modified: `backend/app/services/ingestion_jobs.py`,
  `backend/app/services/vector_store.py`,
  `backend/app/services/vector_search.py`, `backend/app/services/rag_service.py`,
  `backend/app/prompts/rag_prompt.py`, `backend/app/reindex.py`,
  `backend/README.md`, `backend/tests/test_multitenant_architecture.py`,
  `backend/tests/test_qdrant_vector_store.py`, and
  `backend/tests/test_chat.py`.
- Canonical `/api/documents/upload` ingestion now extracts and validates content,
  writes authoritative chunk text plus deterministic point IDs with `pending`
  status to SQLite, and commits that transaction before embedding generation.
  Embeddings are generated in configured-size batches, upserted to Qdrant, and
  chunks are marked `completed` with `qdrant_indexed_at` only after the provider
  confirms the write.
- New canonical chunks deliberately keep `chunks.embedding` null. Historical
  JSON embeddings remain readable for backward compatibility, and the reindex
  command regenerates missing vectors from authoritative SQLite text.
- Qdrant payloads no longer duplicate complete chunk text. Search receives
  `chunk_id`, preserves Qdrant ranking, and hydrates text, filename, and source
  metadata from SQLite under a second organization, document ACL, active-row,
  and searchable-version check. A test corrupts vector-side text and confirms it
  cannot override the SQLite source of truth.
- Retry behavior is deterministic and loss-safe. Fully written Qdrant vectors
  are retrieved by point ID and finalized without re-embedding. A missing or
  partial Qdrant write causes extraction and batch embedding to run again, then
  idempotently overwrites the same UUIDv5 point IDs. Both chunks and
  `document_contents` are finalized together after interrupted indexing.
- Retrieval applies `RAG_MIN_SCORE` in Qdrant and repeats the threshold check in
  application code. No reranker is enabled, so there is no post-reranker stage;
  a future reranker must add its own evaluated relevance threshold.
- When no authorized candidate survives, chat returns exactly
  `The requested information is not available in the uploaded documents.` with
  `sources: []` and `grounded: false`, without reserving or calling the LLM.
  The system prompt independently prohibits missing-fact inference, general
  knowledge, and invented values, names, dates, totals, tables, or citations.
- Security: vector queries retain organization and owner/visibility payload
  filters; SQLite hydration rechecks tenant, ACL-derived document/version IDs,
  active document state, and active chunk state. No document text, vector, path,
  secret, or provider exception is added to logs.
- Compatibility: the frontend already uses the canonical asynchronous endpoint,
  so no frontend change was required. The explicitly named
  `/documents/upload-legacy` compatibility endpoint retains its historical
  synchronous SQLite-vector behavior and is not part of this canonical worker
  migration.
- Verification: the complete backend suite passed all 85 tests in 289.334
  seconds. Coverage includes pre-embedding commit visibility, null SQLite
  embeddings, Qdrant payload minimization and vector recovery, provider-failure
  retries, deleted-content re-upload, authoritative hydration, score thresholds,
  and the no-context/no-LLM branch.
- Rollback: revert the application and test files in this entry together. No
  destructive data conversion occurred; historical embedding JSON remains
  intact, and Qdrant point IDs and collection dimensions are unchanged.

## 2026-07-30 13:11:04 +05:30 — One-time SQLite vector cutover command

- Added `backend/scripts/migrate_vectors_to_qdrant.py` and the scripts package.
  The command is read-only unless `--apply` is explicitly supplied and emits a
  content-free JSON report suitable for deployment evidence.
- Selection is limited to active chunks whose document content and completed
  current version are also active. An optional `--organization-id` supports a
  controlled tenant-by-tenant rollout.
- Legacy SQLite vectors are reused only when the JSON is a finite numeric list
  whose recorded model and dimension match current settings. Invalid, missing,
  stale-model, or wrong-dimension vectors are regenerated from authoritative
  SQLite chunk text in configured embedding batches.
- Every point receives the existing deterministic UUIDv5 ID contract. The
  command preflights ID ownership, never clears Qdrant, performs idempotent
  batch upserts, verifies every expected point and vector dimension, then runs
  tenant-, owner/visibility-, document-, and version-filtered smoke queries.
- SQLite IDs are assigned with `pending` state before provider writes. Rows move
  to `completed` with `qdrant_indexed_at` only after all point verification and
  smoke queries succeed. Any generation, upsert, verification, or query failure
  marks the selected rows `failed` and leaves the command safe to rerun.
- The script does not delete or rewrite `chunks.embedding`; the JSON remains
  available through at least one validated release and rollback exercise.
  Runtime ingestion and retrieval no longer read it. Transitional migration and
  reindex utilities retain backward-compatible read support.
- Added configurable `QDRANT_PREFER_GRPC`. It defaults to false so existing
  HTTPS-only deployments remain compatible; production operators can enable it
  only when a protected remote gRPC endpoint is available.
- Updated `backend/README.md` and `.env.example` with local embedded Qdrant,
  authenticated remote Qdrant, coordinated backup, dry-run/apply commands,
  rollback retention, network exposure, secret handling, authenticated identity,
  and component-responsibility guidance.
- Added `backend/tests/test_vector_migration.py`. Tests confirm dry-run behavior,
  valid-vector reuse, invalid-vector regeneration, stable IDs, exact point
  verification, smoke search, completion timing, failure recording, and
  preservation of both valid and invalid legacy JSON.
- Verification: all 87 backend tests passed in 225.658 seconds. Focused migration
  and Qdrant tests passed, Python compilation succeeded, and
  `git diff --check` reported no whitespace errors.
- The command was not run with `--apply` against the workspace database or any
  production Qdrant instance. Deployment execution requires a coordinated
  backup and maintenance window.
- Rollback: keep the SQLite vector column and coordinated SQLite/Qdrant/object
  backups. Revert application code and restore the matched backups if validation
  fails. Column removal belongs to a later, separately tested schema release.

## 2026-07-30 14:38:01 +05:30 — Qdrant rollout verification and rollback controls

- Files added: `backend/tests/test_qdrant_integration.py`,
  `backend/scripts/check_vector_consistency.py`, and
  `backend/scripts/compare_vector_retrieval.py`.
- Files updated: `backend/app/config.py`,
  `backend/app/services/vector_store.py`,
  `backend/app/services/ingestion_jobs.py`, `backend/app/routes/health.py`,
  `backend/.env.example`, `backend/README.md`,
  `backend/tests/test_qdrant_vector_store.py`, and
  `backend/tests/test_vector_migration.py`.
- `/health` now verifies SQLite and the selected vector provider. With Qdrant
  active it returns the requested `healthy`/`connected` response.
  `/health/ready` retains detailed provider status; Qdrant health now includes
  collection, point count, configured vector size, and actual payload-index
  names.
- Added the temporary `VECTOR_STORE=qdrant|sqlite` rollout flag while retaining
  `VECTOR_STORE_PROVIDER` as an alias. `SQLiteVectorStore` implements retained
  JSON-vector upsert, cosine search, deterministic ID lookup, lifecycle-safe SQL
  filtering, reconciliation, and health reporting.
- Added `VECTOR_STORE_ROLLBACK_DUAL_WRITE`. When explicitly enabled, a successful
  Qdrant ingestion stores a rollback vector in SQLite only after Qdrant confirms
  the upsert. It is also populated during interrupted-finalization recovery.
  Default code behavior remains false; `.env.example` enables it for the
  validation release and documents later removal.
- Both Qdrant and SQLite providers derive identity from backend-authenticated
  user state. SQLite fallback search repeats organization, owner/visibility,
  active-document, active-chunk, and allowed-current-version predicates. The
  frontend chat schema has no `owner_id` or `organization_id` override.
- The read-only consistency command compares active completed current-version
  SQLite IDs with active vector-store IDs. It reports exact missing/unexpected
  IDs, unique content-chunk count, and duplicate content-point count without
  exposing text.
- The comparison command requires 20–30 JSONL questions and records expected
  chunk, SQLite/Qdrant top chunk, score, correctness, and response time to CSV.
  It validates that the supplied user belongs to the requested organization
  before reading any retrieval scope.

### Isolated startup and Phoenix results

- Started the FastAPI lifespan against an isolated SQLite database and real
  in-memory Qdrant client. This avoided deleting or opening the workspace's
  persistent Qdrant directory.
- Confirmed collection creation, configured vector size, successful `/health`,
  SQLite document/content/chunk rows, deterministic point IDs, matching Qdrant
  IDs, and completed ingestion.
- The Phoenix manager question retrieved the chunk containing Anitha. The phone
  question returned the exact unavailable-information response and the test
  proved the LLM was not called.
- A private User A document was returned to User A and excluded for User B.
  The same isolation remained valid after switching to SQLite rollback.
- Soft deletion marked SQLite state, excluded the document from retrieval, and
  retained its audit event.
- Deleted-content re-upload reused the prior vector values without calling the
  embedding generator and retained the same number of active points. Retrying a
  completed ingestion updated deterministic IDs without increasing total point
  count.
- Qdrant search outage returned a safe 502 without provider details. Wrong
  dimensions were rejected. Embedding/provider failures, worker recovery, and
  retry scheduling remain covered by the broader suite. A partial migration
  batch failure was rerun successfully with stable IDs.
- Existing automated format coverage passed for TXT, PDF, DOCX, XLSX, PPTX, and
  image/OCR parsing, including page, slide, sheet/cell/row, and OCR provenance.
  Workbook tests process all 13 supplied tabs, which is stronger than the
  requested three-sheet check.

### Unverified production gates and discrepancies

- No authenticated remote Qdrant URL/API key was configured. Remote collection
  creation and payload-index creation remain implemented, but actual server or
  Cloud connectivity, effective remote indexes, networking, backups, and
  restart persistence were not verified. Embedded Qdrant explicitly does not
  implement effective payload indexes.
- The canonical async endpoint still treats an active same-content upload under
  a different filename as `DOCUMENT_ALREADY_EXISTS`. The requested Case C
  expects a second document reference with no additional content-scoped points.
  Current deterministic IDs are version-scoped by prior design, so changing this
  requires a deliberate shared-point/reference redesign across ingestion,
  deletion, ACL filtering, and hydration. It was not silently changed here.
- The 20–30-question quality comparison was not run because no curated question
  file and expected source IDs were supplied. The new command is ready, but
  Qdrant should not be declared production-primary until its result is reviewed.
- No coordinated SQLite/Qdrant/object-storage backup or production feature-flag
  switch was performed. The workspace database and persistent Qdrant directory
  were not mutated.
- Verification: all 98 backend tests passed in 298.704 seconds; the 14 focused
  Qdrant/migration tests passed in 107.828 seconds. CLI help, Python compilation,
  and whitespace validation passed.

## 2026-07-30 15:30 +05:30 — CSV range retrieval and citation consistency

### Confirmed root cause

The investigation confirmed two related defects before the fix:

1. `Show all equipment priced between ₹50,000 and ₹200,000.` is an exhaustive
   structured filter, but `is_analytical_question()` returned false. It did not
   recognize `show all`, `between … and …`, or `from … to …`. CSV ingestion also
   omitted `workbook_sheets` and `workbook_rows`, so the request used semantic
   top-K retrieval rather than deterministic row filtering.
2. When retrieval returned any source, `rag_service.answer_question()` called
   the LLM and unconditionally returned those sources with `grounded=true`.
   This also happened when the LLM returned the exact unavailable-information
   sentinel. The frontend then rendered every returned source without checking
   `grounded`.

The exact contradictory-response component was
`backend/app/services/rag_service.py`, `answer_question()` (the post-LLM return
path, currently near line 118). The wrong query route originated in
`backend/app/services/workbook_analysis.py`, `is_analytical_question()` (line
63), and `backend/app/services/ingestion_jobs.py`, `_extract_bundle_child()` /
`_extract_bundle()` (lines 62/96), which limited structured extraction to
XLS/XLSX.

### End-to-end trace and evidence

- `AppContext.sendMessage()` sends the question and optional collection/document
  scope. `routes/chat.py` derives the user from authentication; the client
  cannot override owner or organization.
- Before the fix the classifier returned false. RAG requested 15 Qdrant
  candidates, applied score threshold 0.35, and retained at most 5.
- Qdrant applies threshold plus tenant/ACL/current-version/deletion filters.
  `vector_search.py` repeats the score check, validates payload identity against
  SQLite, and hydrates authoritative text/provenance. Discarded chunks do not
  reach the response. There is no reranker in this path.
- `source_extraction._csv()` created one chunk per physical row. The seven-row
  fixture stored the header as row 1 and data separately. Row 5 was
  `Irrigation Pump<TAB>75000<TAB>Irrigation`; the header was not attached to
  that chunk. Filename and `row_start=row_end=5` were retained.
- A controlled pre-fix trace supplied this hydrated row-5 candidate at score
  0.61. One candidate passed threshold and reached the prompt. The exact prompt
  contained that single source. The LLM path executed and returned the
  unavailable sentinel. The final pre-fix JSON still contained the row-5 source
  and `grounded=true`, reproducing the reported UI.
- With no candidates, the backend already short-circuited before the LLM. The
  incident was the distinct context-present/LLM-insufficient path.
- React mapped `response.sources` unconditionally and attached the first source
  to the message. Previous retrieval-panel state also remained until a success,
  which could show stale side-panel state after an API error; the reproduced
  row-5 citation itself came from the same backend response.

Workspace logs contained no matching production request or Qdrant result.
Therefore no production vector ID, raw pre-threshold count, or production score
is claimed. The 0.61 value is controlled reproduction evidence. Persistent logs
were not changed to include raw payloads, prompts, or document text because
they can expose tenant data; restricted diagnostics should use opaque IDs,
scores, and provenance with text disabled or redacted.

### Fix

- `workbooks.py` safely parses CSV using UTF-8 BOM handling and dialect
  detection, then reuses the existing typed header/row model.
- `ingestion_jobs.py` persists CSV structured sheets/rows alongside semantic
  chunks. Qdrant IDs, ACLs, ownership, soft deletion, and duplicate reuse are
  unchanged.
- `workbook_analysis.py` recognizes explicit ranges, dynamically resolves the
  numeric column (including price aliases), parses grouped currency values as
  `Decimal`, applies an inclusive full-row filter, and cites selected row bounds.
- `rag_prompt.py` defines one canonical unavailable sentinel.
- `rag_service.py` enforces: unavailable answer implies `sources=[]` and
  `grounded=false`, logging `insufficient_context` rather than `success`.
- No threshold was lowered. Free-text retrieval stays on Qdrant; exhaustive
  tabular ranges use authoritative SQLite rows.

### Files inspected and modified

- Frontend: `frontend/src/services/api.ts`,
  `frontend/src/context/AppContext.tsx`, chat/source rendering types.
- API/prompt: `backend/app/routes/chat.py`,
  `backend/app/services/rag_service.py`,
  `backend/app/prompts/rag_prompt.py`.
- Retrieval/config: `backend/app/services/vector_search.py`,
  `backend/app/services/vector_store.py`, `backend/app/config.py`.
- Ingestion/data: `backend/app/services/ingestion_jobs.py`,
  `backend/app/services/source_extraction.py`,
  `backend/app/services/workbooks.py`, `backend/app/database.py`.
- Analysis/tests: `backend/app/services/workbook_analysis.py`; chat, Qdrant,
  workbook, parser/PDF, duplicate, multitenant, upload, and lifecycle tests.

### Validation and risk

- 6 chat tests passed, including no-LLM with no context and
  context-present/unavailable response consistency.
- 7 workbook tests passed in 78.775 seconds. XLSX returned Power Tiller,
  Irrigation Pump, and the inclusive ₹200,000 Harvester row, excluded ₹45,000,
  ₹210,000, and ₹550,000, and cited rows 4–6.
- 11 in-memory-Qdrant lifecycle tests passed in 65.280 seconds. Queued CSV
  ingestion stored seven semantic chunks plus structured rows, bypassed the LLM
  for the range, returned the same three rows, and cited CSV rows 4–6. Relevant
  retrieval, unknown/no-source, two-user isolation, soft delete,
  deleted-content reuse, idempotency, provider failure, and SQLite rollback
  also passed.
- Parser/PDF and duplicate/isolation modules printed `Ran 31 tests … OK` in
  172.369 seconds; the command wrapper timed out during interpreter cleanup
  after the test result.
- Frontend TypeScript/Vite production build passed (2,284 modules); the existing
  large-bundle warning remains.
- Monolithic discovery exceeded the command budget because the Windows
  integration suite takes over five minutes. A new monolithic pass is not
  claimed; affected modules were run separately.
- Existing CSV documents require controlled reindex/re-upload for structured
  rows. Ambiguous numeric columns still request clarification. Rollback is
  application-only and does not alter `VECTOR_STORE` rollback capability.

## 2026-07-30 16:00 +05:30 — Structured indexing migration 010

### Files changed

- `backend/app/database.py`
- `backend/app/services/workbooks.py`
- `backend/app/services/ingestion_jobs.py`
- `backend/tests/test_structured_index_migration.py`
- `backend/tests/test_multitenant_architecture.py`
- `backend/tests/test_qdrant_integration.py`

### Change summary

- Added content-scoped structured indexing status, parser version, completion
  timestamp, and bounded error storage through migration
  `010_structured_csv_indexing`.
- Legacy content defaults to pending and is never marked completed by migration
  inference. This provides a safe candidate state for the future CSV backfill.
- Production structured workbook writes mark the content completed with parser
  version `structured-workbook-v1` inside the existing workbook transaction.
- Added a single partial retry index for active pending/failed content.
- Added a reusable single-line, 500-character sanitizer for safe public
  structured-index errors. Raw exceptions, paths, SQL, document content, and
  provider details must not be stored.
- Existing soft deletion, owner/organization isolation, duplicate-content
  uniqueness, document/content IDs, chunks, embeddings, and Qdrant state are
  unchanged.

### Verification

- 9 relevant database and CSV integration tests passed in 66.563 seconds.
- Focused production CSV status verification passed in 9.700 seconds.
- 4 duplicate-content, owner-isolation, and soft-delete regressions passed in
  20.612 seconds.
- Python compilation and `git diff --check` passed.
- No workspace or production database migration was executed; tests used
  isolated SQLite databases.

## 2026-07-30 16:10 +05:30 — Reusable structured CSV ingestion service

### Files changed

- `backend/app/services/structured_ingestion.py` (new)
- `backend/app/services/ingestion_jobs.py`
- `backend/tests/test_structured_index_migration.py`

### Change summary

- Added `StructuredDocumentContext` and `ingest_structured_csv()` as the single
  reusable entry point for normal CSV ingestion and the future legacy backfill.
- The service validates the exact document, version, content, owner, and
  organization relationship against active SQLite rows. It also verifies the
  supplied path matches the version's traversal-safe storage key, repeats the
  stored-file hash/signature checks, and calls the existing
  `workbooks.extract_workbook()` parser.
- Moved sheet/row replacement into `replace_workbook_content()`. Cell values
  remain stored in the existing `workbook_rows.values_json` representation; no
  second CSV parser or parallel schema was introduced.
- Both persistence functions require an existing caller transaction and never
  begin, commit, or roll it back. Delete-and-reinsert replacement is therefore
  atomic and safe for retrying incomplete structured data.
- The normal queued CSV worker now uses this service. XLS/XLSX retain their
  existing extraction behavior and use the same context-validated persistence
  function.
- The service creates no documents or content records and does not import or
  call embeddings or the vector store. Existing chunks, point IDs, Qdrant
  vectors, duplicate relationships, and soft-delete behavior are unchanged.

### Verification

- 4 structured migration/service tests passed in 20.431 seconds, including
  caller-transaction enforcement, incomplete-row replacement, tenant fields,
  status stamping, and confirmation that no chunks were created.
- The normal queued CSV range test and XLSX structured range test both passed
  in 18.874 seconds.
- Python compilation and `git diff --check` passed; only existing CRLF
  conversion warnings were emitted.
- No database migration or schema change was added by this refactor, so
  `changes/changes_db` was not updated.

## 2026-07-30 16:25 +05:30 — Single-document CSV structured reindex

### Files changed

- `backend/app/services/structured_ingestion.py`
- `backend/tests/test_structured_csv_reindex.py` (new)

### Change summary

- Added `reindex_existing_csv_document()` for one existing document at a time.
  Its owner- and organization-scoped lookup validates the document, current
  version, and content relationship without disclosing cross-tenant records.
- Deleted documents, versions, or content are returned as `skipped_deleted`.
  Non-CSV versions are rejected before any structured state is changed.
- The service resolves only the current version's authoritative storage key
  beneath the upload root. The shared ingestion service then verifies the exact
  resolved path, stored hash, and file signature before using the production
  CSV parser.
- One `BEGIN IMMEDIATE` transaction covers each document. Status is set to
  `processing` before parsing; successful sheet/row replacement and the
  `completed` state commit together.
- A savepoint protects structured records during the attempt. On failure,
  sheet/row changes are rolled back, a sanitized bounded `failed` message is
  committed, and the document remains safe to retry. Previously committed
  structured data is preserved when a retry fails.
- Replacement remains content-scoped and idempotent, preserving the existing
  document/content IDs and tenant fields. The service has no embedding or
  vector-store calls, so chunks and Qdrant points are unchanged.

### Verification

- All 7 requested reindex tests passed in 58.186 seconds: success, deleted
  skip, non-CSV rejection, missing stored file, parser rollback, idempotent
  rerun, and unchanged Qdrant point count/no vector or embedding access.
- All 4 structured migration/shared-service tests passed in the focused
  regression command.
- The broader CSV and XLSX integration checks could not start their ingestion
  assertions because this environment does not have the optional
  `qdrant-client` package installed (`ModuleNotFoundError`). This occurred
  during vector-store construction, outside the reindex path; no pass is
  claimed for those two checks.
- Python compilation and `git diff --check` passed. The diff check emitted only
  the repository's existing LF-to-CRLF conversion warnings.
- No database schema change was required, so `changes/changes_db` was not
  updated for this change.

## 2026-07-30 17:04 +05:30 — Structured CSV reindex CLI

### Files changed

- `backend/scripts/reindex_structured_csv.py` (new)
- `backend/tests/test_reindex_structured_csv_script.py` (new)

### Change summary

- Added a batch CLI that scans only active current document/version/content
  relationships whose authoritative stored key ends in `.csv`.
- The script delegates every write attempt to
  `reindex_existing_csv_document()`; it does not accept file paths or duplicate
  parsing, persistence, transaction, embedding, or Qdrant logic.
- Positive document/owner IDs, bounded batch sizes, and conservative
  organization IDs are validated by `argparse`. Invalid arguments exit with
  code 2 before database access.
- Default selection skips current-version completed records, failed records,
  and records already processing. `--retry-failed` admits failed records;
  `--force` admits all matching active CSV records, including completed or
  processing records.
- Dry-run uses SQLite URI read-only mode plus `PRAGMA query_only`; it does not
  initialize/migrate the database or call the reindex service.
- Per-document failures emit only the document ID and a generic message, then
  processing continues. Filenames, paths, CSV content, cell values, and raw
  exception details are not logged.
- The final JSON summary reports `scanned`, `eligible`, `completed`, `skipped`,
  `failed`, `rows_indexed`, and `duration`. Exit code 0 means no document
  failures, 1 means one or more document failures, 2 means invalid CLI usage,
  and 3 means the configured database could not be safely accessed.
- `--version` prints both CLI version `1.0` and the current structured index
  version (`structured-workbook-v1`).

### Command examples

Run these commands from the `backend` directory:

```text
python -m scripts.reindex_structured_csv --dry-run
python -m scripts.reindex_structured_csv --batch-size 100
python -m scripts.reindex_structured_csv --retry-failed --batch-size 50
python -m scripts.reindex_structured_csv --document-id 42 --owner-id 10 --organization-id org-a
python -m scripts.reindex_structured_csv --document-id 42 --owner-id 10 --organization-id org-a --force
python -m scripts.reindex_structured_csv --version
```

### Verification

- All 4 focused CLI tests passed in 0.074 seconds, covering query-only
  read-only dry-run behavior, no service call during dry-run, current-version
  skip logic, retry/force eligibility, continuation after failure, and row
  totals.
- Help and version output completed successfully using the documented module
  invocation.
- Python compilation and `git diff --check` passed. The diff check emitted only
  the repository's existing LF-to-CRLF conversion warnings.
- No database schema change was required, so `changes/changes_db` was not
  updated.

## 2026-07-30 17:08 +05:30 — Structured CSV CLI dry-run coverage

### Files changed

- `backend/tests/test_reindex_structured_csv_script.py`

### Test coverage

- Added isolated integration fixtures for active pending CSVs, a completed CSV
  at the current structured index version, a soft-deleted CSV, a non-CSV
  document, two owners, and two organizations.
- Tests patch `DATABASE_PATH` and `UPLOAD_DIRECTORY` to a temporary directory.
  They never open the production SQLite database or a persistent Qdrant path.
- A complete before/after snapshot confirms dry-run does not change structured
  statuses, versions, timestamps, or errors and creates no workbook sheets or
  rows. The current schema stores cell values inside
  `workbook_rows.values_json`; the test also counts `workbook_cells` when that
  table exists, preserving coverage if a dedicated cell table is added later.
- The reindex write service is guarded with an immediate assertion failure.
  The vector-store accessor is also guarded and verified as never called, and
  fake existing point state plus all upsert/delete mutation counters remain
  unchanged.
- The unfiltered dry-run scans four active CSV documents: three are eligible
  and the current-version completed document is skipped. The soft-deleted CSV
  and active non-CSV document are excluded from the scan.
- Owner and organization filters return only their matching tenant records;
  a mismatched owner/organization pair returns zero records.
- Invalid document IDs, owner IDs, organization IDs, and batch sizes exit with
  argparse code 2 before database or reindex-service access.

### Verification

- All 7 structured CSV CLI tests passed in 16.390 seconds, including the three
  new isolated database-backed dry-run tests.
- `git diff --check` passed with only the repository's existing LF-to-CRLF
  conversion warnings.

## 2026-07-30 17:12 +05:30 — CSV reindex idempotency and retry coverage

### Files changed

- `backend/tests/test_reindex_structured_csv_script.py`

### Test coverage

- Added an isolated end-to-end CLI lifecycle test using a temporary SQLite
  database, temporary upload directory, and a validated stored legacy CSV.
- The initial default run completes with one logical workbook, one sheet, two
  rows, and four cell values. The current schema represents the logical
  workbook by its content-scoped sheets and stores cells in row JSON.
- A second default run scans the document but skips it because its completed
  structured index version matches `STRUCTURED_INDEX_VERSION`. Record counts
  remain unchanged.
- `--force` reprocesses the current-version document through the production
  service. Atomic replacement leaves all workbook/sheet/row/cell counts
  unchanged, proving no duplication.
- Changing the stored structured index version to an older value makes the
  document eligible without `--force`. Reindex completes and restores the
  current version without duplicating records.
- A temporary SQLite trigger aborts insertion of a structured row after
  replacement has started. The service reports one failure, the savepoint
  restores the prior sheets and rows, and content status becomes `failed`.
- The persisted error is the bounded, single-line generic message
  `Structured CSV reindexing failed.`; injected database details are not
  exposed.
- After removing the test trigger, `--retry-failed` completes successfully and
  restores `completed`, the current structured index version, and a null error.
- Qdrant access is blocked for the entire lifecycle. The vector-store accessor
  is never called and the fake point count remains one at all seven
  checkpoints, covering initial, repeat, force, version upgrade, failure, and
  retry behavior.

### Verification

- The new lifecycle test passed independently in 6.553 seconds.
- The complete focused CLI and one-document reindex suite passed all 15 tests
  in 90.071 seconds.
- `git diff --check` passed with only the repository's existing LF-to-CRLF
  conversion warnings.

## 2026-07-30 17:17 +05:30 — Shared-content legacy CSV reindex coverage

### Files changed

- `backend/tests/test_reindex_structured_csv_script.py`

### Duplicate-policy finding

- No schema or ownership conflict was found. The existing partial unique index
  permits one active `document_contents` identity per owner and normalized
  content hash. Multiple legacy document/version references can safely point
  to that same content identity.
- Structured workbook sheets and rows are intentionally content-scoped and
  carry the content owner's `owner_id` and `organization_id`; they are not
  duplicated per document reference. No schema redesign was made.
- Existing upload policy remains unchanged: a new identical active upload is
  terminal unless the approved ingestion path explicitly reuses an existing
  content identity. Reindexing does not participate in duplicate resolution.

### Test coverage

- Added two active legacy CSV document/version references with identical files,
  one shared active content row, and the existing duplicate marker on the
  second document.
- The first CLI pass processes both legacy references but converges on one
  logical workbook, one sheet, two rows, and four cell values. The
  `document_contents` count and both existing chunk/vector point IDs remain
  unchanged.
- The next default pass skips both current-version references. A later forced
  pass also replaces the shared structured data without duplication.
- The test soft-deletes one document, its version, and its document-scoped
  chunk, then runs the production orphan-content cleanup rule. The other
  document/version remains active and valid, so the shared content and
  structured data remain active and queryable.
- A separate same-hash content/document/version fixture that was already
  soft-deleted remains deleted through every default and forced reindex pass;
  it is never reactivated.
- Workbook sheets and rows retain exactly `(owner_id=10,
  organization_id='org-a')`, matching the shared content and both document
  references.
- Qdrant access is blocked during reindex. The accessor is never called, the
  fake point count remains two, and SQLite vector point IDs are unchanged.

### Verification

- The new shared-content integration test passed independently in 4.557
  seconds.
- The focused CLI, reindex service, duplicate-version, deleted-content reuse,
  and cross-user isolation suite passed all 19 tests in 92.074 seconds.
- `git diff --check` passed with only the repository's existing LF-to-CRLF
  conversion warnings.
- No database or application schema files were changed.

## 2026-07-30 17:22 +05:30 — Legacy CSV reindex retrieval coverage

### Files changed

- `backend/tests/test_legacy_csv_reindex_retrieval.py` (new)

### Test coverage

- Added an isolated legacy CSV with an active document/content/version and no
  initial workbook sheets or rows. The fixture uses a temporary SQLite
  database and upload directory.
- Before reindex, `has_structured_workbook()` is false and the analytical range
  question cannot enter deterministic structured filtering. With no semantic
  result, the request returns the standard ungrounded unavailable response.
- The production single-document reindex service creates one structured CSV
  sheet and all seven source rows without accessing Qdrant.
- The exact question `Show all equipment priced between ₹50,000 and
  ₹200,000.` is asked twice. Both responses are identical, semantic retrieval
  and the LLM are explicitly prohibited, and the structured result contains
  exactly four matches.
- Boundary Sprayer at ₹50,000 and Harvester at ₹200,000 prove that both range
  boundaries are inclusive. Power Tiller and Irrigation Pump are also
  returned. Seed Drill, Agricultural Drone, and Tractor are excluded.
- The single CSV citation identifies the correct document/display filename,
  stored CSV sheet name, and matching source rows 3 through 6.
- The matching response is grounded. An unavailable warranty question returns
  exactly `The requested information is not available in the uploaded
  documents.`, with `sources=[]` and `grounded=false`.
- A second owner in the same organization and a user in another organization
  cannot access the private workbook. Both receive the same ungrounded
  unavailable response with no sources.

### Verification

- The new integration test passed independently in 3.778 seconds.
- The complete focused legacy retrieval, CLI reindex, and single-document
  reindex suite passed all 17 tests in 47.528 seconds.
- `git diff --check` passed with only the repository's existing LF-to-CRLF
  conversion warnings.

## 2026-07-30 17:48 +05:30 — Complete CSV structured reindex validation

### Isolation

- Validation used
  `.validation/csv_structured_full_20260730_1725` for count instrumentation.
  Backend tests used their own temporary SQLite databases, upload directories,
  and in-memory/guarded Qdrant stores.
- `QDRANT_PATH` and `QDRANT_LOCAL_PATH` were set to the temporary validation
  Qdrant directory for every backend command. Production SQLite and persistent
  Qdrant data were not opened.
- The temporary validation directory was verified, removed recursively after
  final counts, and confirmed absent.

### Exact validation commands

Run from `backend` unless another directory is shown:

```text
python -m pip install qdrant-client==1.18.0
python -m pip install pypdf==5.9.0
python -m unittest tests.test_structured_index_migration -v
python -m unittest tests.test_structured_csv_reindex tests.test_reindex_structured_csv_script tests.test_legacy_csv_reindex_retrieval -v
python -m unittest tests.test_document_parsers tests.test_workbook_domain_neutral -v
python -m unittest tests.test_chat -v
python -m unittest tests.test_qdrant_vector_store tests.test_qdrant_integration tests.test_vector_migration -v
python -m unittest tests.test_document_duplicates tests.test_multitenant_architecture -v
python -m compileall -f app scripts tests
```

Run from `frontend`:

```text
npm run build
```

Run from the repository root:

```text
git diff --check
```

Each backend validation command also set:

```text
QDRANT_PATH=.validation/csv_structured_full_20260730_1725/qdrant
QDRANT_LOCAL_PATH=.validation/csv_structured_full_20260730_1725/qdrant
PYTHONUNBUFFERED=1
```

### Results and runtimes

- Database migration: 4 passed, 0 failed, 0 skipped; 25.405 seconds.
- CSV structured ingestion/reindex/retrieval: 17 passed, 0 failed, 0 skipped;
  64.805 seconds.
- Parser/XLSX/workbook: 20 passed, 0 failed, 0 skipped; 73.967 seconds.
- Chat policy: 6 passed, 0 failed, 0 skipped; 5.021 seconds.
- Qdrant lifecycle/vector migration: 15 passed, 0 failed, 0 skipped; 123.238
  seconds.
- Duplicate, soft-delete, two-user, ACL, and tenant isolation: 33 passed,
  0 failed, 0 skipped; 316.901 seconds.
- Final backend result: 95 passed, 0 failed, 0 skipped. Passing backend command
  runtime totaled 609.337 seconds.
- The first parser/workbook attempt reported 19 passes and one import error
  because the declared `pypdf` dependency was absent. After installing the
  pinned `pypdf==5.9.0`, the complete 20-test category passed. The failed
  attempt took 49.307 seconds and is not hidden or counted as a product-test
  failure.
- Python compilation passed in 4.836 seconds.
- Frontend production build was required because frontend source is modified.
  It passed in 52.361 seconds: TypeScript and Vite transformed 2,284 modules.
- `git diff --check` passed in 0.827 seconds with only existing LF-to-CRLF
  conversion warnings.
- Final successful validation runtime including compilation, frontend build,
  and diff check was 667.361 seconds. Including the dependency-related failed
  parser attempt, command runtime was 716.668 seconds.

### Point and row counts

- Isolated Qdrant count instrumentation: 0 collections/0 points before;
  2 collections/20 points after lifecycle validation. Final collections were
  `rag_chunks` with 15 points and `test_contract` with 5 points.
- CSV structured reindex-specific invariants remained unchanged at every
  assertion: empty case 0→0 points, retry case 1→1 point, and shared-content
  case 2→2 points. Reindex never called the Qdrant accessor.
- Isolated sentinel SQLite counts remained zero before and after for
  `documents`, `document_contents`, `document_versions`, `workbook_sheets`,
  `workbook_rows`, and `chunks`, confirming test isolation.
- The end-to-end legacy CSV fixture retained 1 document, 1 content, and
  1 version; structured rows changed from 0 sheets/0 rows to 1 sheet/7 rows,
  while chunks remained 0.
- The retry/idempotency fixture remained at 1 logical workbook, 1 sheet,
  2 rows, and 4 stored cell values across rerun, force, failure rollback, and
  retry.
- The shared-content fixture retained 2 content identities, one content-scoped
  workbook, 1 sheet, 2 rows, and 4 cell values; reindex created no content or
  vector records.

### Non-failing findings

- `qdrant-client==1.18.0` and `pypdf==5.9.0` were present in requirements but
  missing from the Python environment; both exact pinned versions were
  installed for validation.
- Some suites emitted existing SQLite `ResourceWarning` messages for unclosed
  connections.
- FastAPI emitted the existing TestClient/httpx deprecation warning.
- Worker warm-up logged that `sentence_transformers` was unavailable. Relevant
  tests injected deterministic embeddings and all assertions passed.
- Vite emitted the existing advisory that the 660.18 kB JavaScript chunk
  exceeds 500 kB. The production build still completed successfully.

## Vector Store Inspection - 2026-07-31 14:40:43 +05:30

Scope: Verified the configured vector database by reading the live collection,
checking backend health, counting vectors, and running a safe similarity search.
No API keys, JWTs, or document body text were recorded.

### Active vector store

- Active store: Qdrant local mode, selected by `VECTOR_STORE=qdrant_local`.
- Configured Qdrant mode: `local`.
- Configured storage path: `./qdrant_data`.
- Resolved storage path:
  `C:\Users\aparna_adevtechcorp\Desktop\RAG PROJECT\backend\qdrant_data`.
- Configured Qdrant URL: empty; no Qdrant server URL is active.
- Collection name: `rag_chunks`.
- Provider health call returned: provider `qdrant`, mode `local`, status `ok`,
  vector size `384`.

### Startup and live connectivity

- Backend startup import check passed: `app.main` imported successfully.
- Vector store initialized during startup import and returned status `ok`.
- Live API health check at `http://127.0.0.1:8000/health` returned healthy with
  database connected and Qdrant connected.
- Live readiness check at `http://127.0.0.1:8000/health/ready` returned ready.
- The collection was not treated as connected merely because the package exists;
  it was opened, counted, and searched successfully.

### Vector counts

- Active collection vector count: `72`.
- Active payload count from `list_active_points()`: `72`.
- Vectors belonging to `Support Hours APARNA_july.xlsx`: `5`.

### Safe similarity-search test

- Query used for test: a non-secret task/support-hours query.
- Retrieved vector count: `5`.
- Top similarity score: `0.3683`.
- Top source document: `Support Hours APARNA_july.xlsx`.

### DB state for `Support Hours APARNA_july.xlsx`

- Document id: `3`.
- Content id: `3`.
- Current version id: `3`.
- `processing_status`: `completed`.
- Workbook structured-row count: `4`.
- Chunk count: `5`.
- Completed chunk count: `5`.
- Vector count in active collection for this workbook: `5`.

### Logs and indexing errors

- No persistent application `.log` files were found under `backend\data`.
- Operational ingestion records were checked in SQLite.
- Latest Support Hours ingestion job status: `completed`.
- Support Hours ingestion job `vector_upsert_failures`: `0`.
- No ingestion jobs were found with vector or Qdrant error codes/messages.
- Active chunk indexing summary: `72` completed chunks, no active failed chunks.
- Support Hours chunk indexing summary: `5` completed chunks, each with a vector
  point id and Qdrant indexed timestamp.

## Chat Unavailable/Wrong-Document Investigation - 2026-07-31 14:58:05 +05:30

Scope: Traced `What task did Aparna do in July?` and related Support Hours
queries through `/chat`, structured workbook analysis, Qdrant retrieval, and
final answer routing. Diagnostics were run server-side with redacted previews;
no API keys, JWTs, full prompts, stack traces, or full workbook rows were
recorded.

### Summary diagnosis

Current code no longer returns `information not available` for every Support
Hours task query, but the exact query `What task did Aparna do in July?` is
still misrouted. The valid Support Hours data is lost before Azure because
`analyze_workbook_question()` calls `_structured_lookup_response()` across all
workbook scopes before selecting the workbook named in the query. The generic
lookup parser treats the preposition `in` from `in July` as a cell value and
creates a false filter `Column 2=in` for `Attentence.xlsx`. That false lookup
returns attendance names and preempts the later workbook-selection path that
would have selected `Support Hours APARNA_july.xlsx`.

If the structured path is skipped, Qdrant does retrieve Support Hours chunks,
but the top score for the exact query is `0.1331`, below `RAG_MIN_SCORE=0.30`.
The retrieval path therefore rejects all chunks and returns the unavailable
answer before any Azure prompt is built.

### Stage table

| Stage | Pass/Fail | Evidence | Root cause |
|---|---|---|---|
| 1. Query received by `/chat` | Pass | Live `/chat` accepted `What task did Aparna do in July?` with user 1 token. | Route receives the query correctly. |
| 2. Workbook/document scope selected | Fail for structured lookup order | Available scopes were `Attentence.xlsx` and `Support Hours APARNA_july.xlsx`. `_choose_workbook()` would select `Support Hours APARNA_july.xlsx`, but it is not reached before lookup. | `analyze_workbook_question()` calls `_structured_lookup_response(scopes, question)` before `_choose_workbook()`. |
| 3. Structured workbook analysis invoked or skipped | Invoked, then misrouted | `is_analytical_question=True`; `is_structured_lookup_question=True`; structured analyzer returned `structured_lookup`. | False structured lookup preempts task-list handling. |
| 4. Retrieved Qdrant chunks | Pass when tested directly | Direct Qdrant search at `min_score=0.0` returned 5 chunks. Top: `Support Hours APARNA_july.xlsx`, score `0.1331`, Sheet1 row 3, preview includes headers and the first task row. | Vector DB is usable; retrieval itself is not the outage. |
| 5. Similarity/grounding threshold decision | Fail in retrieval fallback | Threshold `RAG_MIN_SCORE=0.30`; top Support Hours score `0.1331`; thresholded retrieval count `0`. | Useful workbook rows are discarded by semantic threshold if structured analysis does not answer. |
| 6. Structured rows inspected | Fail in filter parsing | Support Hours headers detected: `Task Date`, `Actual hrs`, `Title`, `Title description`; task column `Title`; description column `Title description`; Support Hours parsed filters `{}`. Attendance parsed false filter `Column 2={'in'}` and matched 12 rows. | `_row_filters()` treats short/common values like `IN` as exact matches for the word `in` in the question. No `Aparna` or `July` row filter is extracted from Support Hours columns. |
| 7. Context sent to Azure OpenAI | Not reached | For current exact query, structured lookup returns before LLM call. For retrieval-only path, thresholded source count is 0, so no context is built. | Valid Support Hours row is never sent to Azure for this query. |
| 8. Final answer-policy decision | Fail only on retrieval path; wrong answer on current structured path | Retrieval path reaches `if not sources` and returns unavailable with `grounded=False`. Current `/chat` instead returns `grounded=True` but with `Attentence.xlsx` as the source. | Two separate loss points: false structured lookup from wrong workbook, and vector threshold rejection when structured path is bypassed. |

### Independent layer results

- Structured workbook analyzer directly:
  - Query: `What task did Aparna do in July?`
  - Result: `structured_lookup`, `grounded=True`, source `Attentence.xlsx`.
  - Evidence: lookup plan for `Attentence.xlsx` was `value_column=A`,
    filters `Column 2={'in'}`, 12 matched rows.
  - Evidence: `_choose_workbook()` would select `Support Hours APARNA_july.xlsx`
    if it were reached.
- Vector retrieval directly:
  - Query: `What task did Aparna do in July?`
  - `min_score=0.0`: 5 retrieved chunks.
  - Top score: `0.1331`.
  - Top source: `Support Hours APARNA_july.xlsx`, Sheet1 row 3.
  - Thresholded with `RAG_MIN_SCORE=0.30`: 0 retrieved chunks.
- Final `/chat` endpoint:
  - Query: `What task did Aparna do in July?`
  - Current result: `structured_lookup`, `grounded=True`, source
    `Attentence.xlsx`.
  - Related query `task done during the support hours by aparna` currently
    returns the Support Hours task list because it reaches the newer task-list
    path.

### Exact loss points

- `backend/app/services/workbook_analysis.py:1043-1046`:
  `analyze_workbook_question()` calls `_structured_lookup_response(scopes,
  question)` before workbook selection. This lets an unrelated workbook answer
  first.
- `backend/app/services/workbook_analysis.py:162-188`:
  `_row_filters()` accepts any exact categorical cell value mentioned in the
  query. For `What task did Aparna do in July?`, `_question_mentions_value()`
  matches the common word `in` to the attendance marker value `IN`.
- `backend/app/services/workbook_analysis.py:890-907`:
  `_structured_lookup_response()` iterates all scopes and collects the false
  attendance matches, then returns them as a valid structured lookup.
- `backend/app/services/rag_service.py:79-114`:
  Once structured analysis returns anything, chat orchestration returns that
  result immediately and does not run vector retrieval or Azure generation.
- `backend/app/services/rag_service.py:116-156`:
  If structured analysis is skipped, Qdrant retrieval runs with
  `min_score=settings.rag_min_score`. For this query, `0.1331 < 0.30`, leaving
  `sources=[]`; the code returns `UNAVAILABLE_ANSWER` with `grounded=False`.

### No temporary code logs retained

No temporary diagnostic logging code was added to the application. The root
cause was confirmed through redacted server-side scripts, direct function calls,
and a live `/chat` request. No diagnostic rows, prompts, keys, or stack traces
were exposed to the frontend.

## Structured Workbook Wrong-Document Permanent Fix - 2026-07-31 15:14:23 +05:30

### Root cause fixed

- Structured workbook lookup previously ran globally before workbook scope
  selection, so an unrelated workbook could answer first.
- Common query words such as `in` could be treated as workbook cell values,
  causing attendance `IN` rows to match questions like `in July`.
- Any structured row match was accepted as grounded, even when the match was
  weak and from a poorly scoped workbook.
- Dense table chunks below the global semantic threshold were discarded before
  document-scoped evidence could be checked.

### Files changed

- `backend/app/services/workbook_analysis.py:98-104`: added stop-word and
  status-value controls for row filters.
- `backend/app/services/workbook_analysis.py:215-254`: made row filtering
  column-aware; stop words are ignored, and `IN`/`OUT` status values are allowed
  only for explicit attendance/status questions.
- `backend/app/services/workbook_analysis.py:513-531`: added candidate workbook
  ranking before structured lookup.
- `backend/app/services/workbook_analysis.py:628-650`: added structured lookup
  confidence scoring so weak accidental matches do not return early.
- `backend/app/services/workbook_analysis.py:1036-1059` and
  `backend/app/services/workbook_analysis.py:1194-1204`: scoped structured
  lookup to candidate workbooks before choosing analytical handlers.
- `backend/app/services/rag_service.py:19-31` and
  `backend/app/services/rag_service.py:85-123`: added bounded low-score
  retrieval fallback based on selected document, useful metadata, and query-term
  overlap.
- `backend/app/services/rag_service.py:186-204`: ungrounded structured results
  no longer block retrieval; fallback remains ACL-filtered through
  `search_chunks()`.

### Ranking and threshold policy

- Workbook scope is scored from filename/query overlap, detected relevant
  columns, meaningful filter values, task columns, and attendance/person columns.
- Structured lookup requires a confidence score of at least `3.0`, combining
  selected-workbook confidence, output-column relevance, meaningful filter
  matches, status-column safety, and matched-row count.
- Dense spreadsheet/PDF table chunks still use `RAG_MIN_SCORE` first. If no
  result passes, a low-score fallback may use chunks down to `0.08` only when
  the best candidate has at least two meaningful overlaps with the query and
  other returned chunks stay in the same selected document.
- Unavailable answers continue to return `grounded=false` and `sources=[]`; no
  source is cited when the answer is ungrounded.

### Test results

- `venv\Scripts\python.exe -m unittest tests.test_workbook_domain_neutral`
  passed: 18 tests in 38.031 seconds.
- `venv\Scripts\python.exe -m unittest tests.test_chat`
  passed: 10 tests in 0.029 seconds.
- `venv\Scripts\python.exe -m compileall app` passed.

## Remove Filename Selection Chat Flow - 2026-07-31 15:33:27 +05:30

### Behavior change

- Chat no longer returns workbook/document filename-selection prompts.
- Natural-language questions are answered from automatically ranked accessible
  evidence. Filename/title metadata remains only a ranking signal.
- If structured evidence is weak or ungrounded, chat continues into scoped
  vector retrieval. If no sufficient evidence exists, it returns exactly:
  `Information not available in the uploaded documents.` with
  `grounded=false` and `sources=[]`.
- Follow-up style entity requests such as `Name them` are treated as genuine
  entity-list questions over the strongest scoped source, not as a file-name
  selection request.

### Files changed

- `backend/app/prompts/rag_prompt.py`: updated the unavailable-answer contract
  to the requested exact wording.
- `backend/app/services/workbook_analysis.py:136-145` and
  `backend/app/services/workbook_analysis.py:420-430`: structured workbook
  availability and loading now require completed current document versions.
- `backend/app/services/workbook_analysis.py:480-482`: automatic workbook
  selection replaces the previous filename-selection prompt path.
- `backend/app/services/workbook_analysis.py:503-522`: candidate workbooks are
  ranked by query/content, filename metadata, header relevance, meaningful
  filters, and task/entity evidence.
- `backend/app/services/workbook_analysis.py:664-676`: employee/person/name
  questions, including follow-ups like `Name them`, use structured entity
  detection automatically.
- `backend/app/services/workbook_analysis.py:1184-1208`: structured lookup runs
  only over ranked candidates and no longer returns a clarification prompt.
- `backend/app/services/vector_search.py:34-40`: default vector retrieval now
  searches only completed current document versions.
- `backend/app/services/rag_service.py`: ungrounded structured results no
  longer end the chat flow; retrieval/unavailable policy decides final output.

### Test results

- `venv\Scripts\python.exe -m unittest tests.test_workbook_domain_neutral`
  passed: 19 tests in 40.545 seconds.
- `venv\Scripts\python.exe -m unittest tests.test_chat`
  passed: 11 tests in 0.049 seconds.
- `venv\Scripts\python.exe -m unittest tests.test_legacy_csv_reindex_retrieval tests.test_qdrant_integration`
  passed: 12 tests in 33.886 seconds.
- `venv\Scripts\python.exe -m compileall app` passed.
- `git diff --check` passed; only the repository's existing CRLF conversion
  warnings were reported.

## 2026-08-03 11:17 +05:30 - Generic source selection and aggregate validation hardening

- Confirmed root cause: global structured workbook analysis accepted a count
  plan with no meaningful schema, value, filename, or source evidence. A
  single-character workbook header could score through substring matching, so an
  unrelated workbook could be selected before retrieval considered the relevant
  PDF/document evidence.
- Files modified: `backend/app/prompts/rag_prompt.py`,
  `backend/app/services/workbook_analysis.py`,
  `backend/app/services/rag_service.py`,
  `backend/app/services/chat_context.py`,
  `backend/tests/test_chat.py`,
  `backend/tests/test_legacy_csv_reindex_retrieval.py`,
  `backend/tests/test_qdrant_integration.py`, and
  `backend/tests/test_workbook_domain_neutral.py`.
- Implementation summary: workbook scoring now ignores single-character and
  stop-word tokens, uses token-boundary matching, applies small plural/past-tense
  normalization, and separates source evidence from aggregation ability. Count
  plans now require an entity, resolved filter, numeric filter, or explicit
  record scope. Quantity-style "how many <numeric column>" questions route to a
  sum, filtered status questions count only matching rows, and weak structured
  plans return unavailable with no sources so retrieval can evaluate PDFs and
  other unstructured documents.
- Retrieval summary: low-score fallback remains bounded by lexical overlap, and
  only allows a non-workbook source without overlap when it has a clear semantic
  lead over the next candidate. This prevents unrelated structured workbooks
  from winning just because they support rows or aggregation.
- Context summary: short command-style follow-ups now require no substantive new
  topic token, so topic changes such as "show invoices" are treated as fresh
  source-selection requests instead of stale context reuse.
- Security considerations: ACL, organization, owner, current-version,
  soft-delete, and indexing readiness predicates remain in the existing SQL
  paths. Diagnostics and tests avoid logging document contents, credentials,
  embeddings, or cell values beyond neutral fixture data.
- Original-scenario verification: read-only diagnostics against the local DB
  show `Attentence.xlsx` and `Support Hours APARNA_july.xlsx` both rank with
  zero structured evidence for "how many products rejected" and return
  `Information not available in the uploaded files.` with `grounded=false` and
  `sources=[]`. The bounded retrieval fallback selects
  `FINAL INSPECTION REJECTION 17.01.2024.pdf` page 1 for that question; the
  more ambiguous "what are the rejections" remains unavailable rather than
  guessing.
- Tests run: `venv\Scripts\python.exe -m compileall app tests` passed;
  `venv\Scripts\python.exe -m unittest tests.test_workbook_domain_neutral -v`
  passed, 18 tests in 62.789 seconds after final token changes;
  `venv\Scripts\python.exe -m unittest tests.test_chat -v` passed, 11 tests in
  0.092 seconds after final retrieval changes;
  `venv\Scripts\python.exe -m unittest tests.test_vector_search
  tests.test_qdrant_vector_store tests.test_document_parsers -v` passed, 14
  tests in 8.610 seconds;
  `venv\Scripts\python.exe -m unittest tests.test_legacy_csv_reindex_retrieval
  -v` passed, 1 test in 4.169 seconds;
  `venv\Scripts\python.exe -m unittest discover -s tests -v` passed, 141 tests
  in 371.010 seconds.
- Remaining limitations: the application still cannot compute an exact
  aggregate from incomplete PDF/vector chunks; it retrieves bounded evidence and
  must answer unavailable or non-aggregate unless the cited source contains
  enough complete support.

## Generic Schema-Driven RAG Refactor - 2026-07-31 17:58:59 +05:30

### Architecture change

- Replaced the workbook analyzer with a schema-driven planner in
  `backend/app/services/workbook_analysis.py`.
- The planner infers generic operations from the question: count, total,
  average, min/max, distinct, list/detail, grouped result, and filtered rows.
- Structured workbook/table answers now use all matching rows from the selected
  source instead of relying on whichever vector chunk was retrieved.
- Automatic document selection ranks accessible ready workbooks by filename
  metadata, schema/header relevance, value/date/entity matches, filter quality,
  and plan validity. No filename-selection prompt or file-specific branch was
  added.
- `backend/app/services/rag_service.py` now uses a generic low-score retrieval
  fallback that requires scoped source overlap; the old report-specific fallback
  vocabulary was removed.
- Follow-up context resolution now reads the generic `result_type` stored by
  structured answers, so reference follow-ups use the latest typed result plan.
- Added `rag_structured_result_limit` in `backend/app/config.py` to cap listed
  rows, contributing values, and stored row references.

### Security and evidence controls

- Owner/org ACL checks remain before structured source loading, vector search,
  context persistence, follow-up resolution, and citation.
- Only completed current document versions are eligible for structured analysis
  or retrieval.
- Unavailable answers continue to return the exact standard text with
  `grounded=false` and `sources=[]`.
- Structured output escapes formula-leading cell values and HTML-sensitive text
  before rendering row/value tables.

### Test results

- `venv\Scripts\python.exe -m compileall app` passed.
- `venv\Scripts\python.exe -m unittest tests.test_chat tests.test_workbook_domain_neutral`
  passed: 22 tests in 24.979 seconds.
- `venv\Scripts\python.exe -m unittest tests.test_structured_index_migration tests.test_structured_csv_reindex tests.test_reindex_structured_csv_script tests.test_chat tests.test_workbook_domain_neutral`
  passed: 42 tests in 57.960 seconds.

## Qdrant Test Isolation and Singleton Fix - 2026-07-31 18:23:27 +05:30

### Root cause

- Full discovery initially failed because some tests patched
  `qdrant_local_path` to empty while leaving the active provider as Qdrant.
- Those tests accidentally initialized the real `QdrantVectorStore`, which
  correctly failed fast with `RuntimeError: QDRANT_PATH is required in local
  mode.`
- The concurrent duplicate-upload test hit the same configuration leak from
  worker threads, so both upload threads raised before returning responses.

### Vector-store/test isolation changes

- `tests/test_document_duplicates.py` now runs against the existing SQLite
  rollback vector provider and resets the vector-store singleton in teardown.
- `tests/test_folder_uploads.py` now uses isolated in-memory Qdrant because the
  test upserts while an SQLite write connection is open.
- `tests/test_qdrant_vector_store.py` now creates a unique
  `TemporaryDirectory()` for Qdrant local mode and never reads or deletes any
  production Qdrant path.
- `tests/test_security_limits.py` now simulates the production parser timeout
  process boundary, avoiding Windows sandbox pipe creation while preserving the
  timeout/termination assertion.

### Concurrency protection

- `app/services/vector_store.py` now resets the cached singleton under the same
  lock used by `get_vector_store()`.
- Added `tests/test_vector_store_singleton.py` to verify concurrent
  initialization creates one store instance and all threads receive that same
  instance.
- No global lock was added around normal vector operations.

### Additional structured-query regression fixed

- While running the full suite, two structured CSV range tests exposed a generic
  planner regression: unrelated list questions could select a text/category
  column based only on inferred type.
- `app/services/workbook_analysis.py` now requires real schema/value evidence
  for list-column selection, supports generic numeric range/comparison filters,
  and returns unavailable when an unrelated structured question has no evidence.

### Verification

- Initial baseline: `venv\Scripts\python.exe -m unittest discover -s tests`
  ran 133 tests and failed with 3 failures and 15 errors.
- Post-fix:
  - `venv\Scripts\python.exe -m compileall app tests` passed.
  - `venv\Scripts\python.exe -m unittest tests.test_document_duplicates`
    passed: 18 tests in 24.227 seconds.
  - `venv\Scripts\python.exe -m unittest discover -s tests`
    passed: 134 tests in 112.938 seconds.
  - `git diff --check` passed; only existing CRLF conversion warnings were
    reported.

### Intentional simulated failures in logs

- `qdrant temporarily unavailable` is an intentional mocked retry simulation.
- `safe parser failure` is an intentional mocked parser failure.
- `An identical active document already exists` is an intentional duplicate
  detection simulation.

## Conversational Follow-Up Context - 2026-07-31 15:49:56 +05:30

### Missing field/path fixed

- Missing request field/path: `frontend/src/context/AppContext.tsx` created a
  stable local `conversationId`, but `frontend/src/services/api.ts` did not send
  it and `backend/app/routes/chat.py` did not accept `conversation_id`.
- Because `/chat` received only the new question, the backend had no safe
  server-side antecedent for short reference questions such as `what are they?`.

### Behavior change

- Grounded answers now save compact, owner/org-scoped follow-up context by
  conversation: previous question, previous grounded answer, cited
  document/version IDs, sources, and bounded structured row references.
- Follow-up/reference questions are resolved before global retrieval using
  pronoun/reference detection, short-query detection, and recent grounded
  context.
- Structured total/count/entity answers preserve the row references needed to
  list or count the same contributing records later.
- Internal `_context` metadata is stripped before the API response is returned;
  no prompts, API keys, or full document bodies are stored.
- Context is isolated by organization, owner, and `conversation_id`; New Chat
  creates a new `conversation_id`, so old context is not reused.

### Files changed

- `backend/app/services/chat_context.py:30-37`: generic follow-up detection.
- `backend/app/services/chat_context.py:99-166`: bounded grounded-context save
  with ACL validation and expiry.
- `backend/app/services/chat_context.py:279-307`: follow-up resolution from the
  latest valid context.
- `backend/app/services/workbook_analysis.py`: structured responses now include
  server-only row-reference context for counts, totals, lookups, people lists,
  and task lists.
- `backend/app/services/rag_service.py`: follow-ups resolve before normal
  retrieval; grounded answers save context; `_context` is removed before
  returning.
- `backend/app/routes/chat.py:18` and `backend/app/routes/chat.py:48`:
  `conversation_id` is accepted and forwarded.

### Test results

- `venv\Scripts\python.exe -m unittest tests.test_workbook_domain_neutral`
  passed: 23 tests in 39.583 seconds.
- `venv\Scripts\python.exe -m unittest tests.test_chat`
  passed: 11 tests in 0.030 seconds.
- `venv\Scripts\python.exe -m compileall app` passed.
- `npm run build` passed for the frontend.
- `git diff --check` passed; only the repository's existing CRLF conversion
  warnings were reported.

## Stale Follow-Up Context Fix - 2026-07-31 16:11:49 +05:30

### Investigation

- Live DB inspection of `backend/data/rag_new.db` found zero active
  `chat_contexts` records, so the latest grounded answer shown in the browser
  had not produced a saved server-side result plan.
- Candidate prior grounded turns considered for the failing `name them` request:
  none were present in `chat_contexts`; no timestamps, document IDs, or result
  plans existed for that active conversation.
- Because no valid context was available, the previous `resolve_follow_up()`
  condition returned `None` when `conversation_id` was missing. The request then
  fell through to normal structured workbook analysis, where `name them` was
  treated as an entity-list question and selected the Attendance workbook.
- Exact stale-context selection condition:
  `backend/app/services/chat_context.py` returned `None` for a reference-style
  follow-up when `conversation_id` was absent, allowing unrelated global
  workbook analysis to answer from an older/irrelevant Attendance source.
- Secondary risk found: latest context lookup ordered by `created_at DESC`.
  SQLite `CURRENT_TIMESTAMP` has second-level precision, so rapid grounded turns
  could tie and select an older row nondeterministically.

### Implementation

- `backend/app/services/chat_context.py:198`: latest context lookup now orders
  by autoincrement `id DESC`, ensuring the newest saved grounded turn wins
  within the same owner/org/conversation.
- `backend/app/services/chat_context.py:304`: reference-style follow-ups with
  no `conversation_id` now return the standard unavailable response instead of
  falling through to global retrieval or workbook analysis.
- `backend/app/services/chat_context.py:112-126`: source/result-plan mismatch is
  rejected at save time when structured row-plan document IDs are outside cited
  source document IDs.
- `backend/app/services/workbook_analysis.py:1177` and
  `backend/app/services/workbook_analysis.py:1224-1229`: stored structured
  plans now include typed `result_type`, display column, and bounded
  contributing values so totals/counts resolve follow-ups from their own result
  schema, not from generic people-list behavior.
- `backend/app/services/workbook_analysis.py:482-487`: multi-workbook automatic
  structured selection now requires minimum confidence, preventing weak numeric
  accidents such as unrelated column `A` from becoming a grounded total.

### Test results

- `venv\Scripts\python.exe -m unittest tests.test_workbook_domain_neutral`
  passed: 26 tests in 72.587 seconds.
- `venv\Scripts\python.exe -m unittest tests.test_chat`
  passed: 11 tests in 0.085 seconds.
- `venv\Scripts\python.exe -m compileall app` passed.
- `git diff --check` passed; only the repository's existing CRLF conversion
  warnings were reported.
2026-08-03 12:23:55 +05:30

- Confirmed root cause: chat selected structured workbook analysis before comparing all eligible evidence, so an unrelated workbook could win when a generic analytical phrase looked executable. Structured plans also allowed row-count style answers without enough source/entity/filter proof, and citations could point at a broad row span without explicit contributing ranges.
- Files modified: `backend/app/services/source_selection.py`, `backend/app/services/rag_service.py`, `backend/app/services/workbook_analysis.py`, `backend/tests/test_source_selection_gate.py`, `backend/tests/test_chat.py`, `backend/tests/test_workbook_domain_neutral.py`, `backend/tests/test_legacy_csv_reindex_retrieval.py`.
- Implementation summary: added one reusable source-selection gate for chat answers; the gate filters explicit documents for ACL/active/readiness, scores structured and unstructured candidates with boundary-token evidence, rejects weak/ambiguous candidates with reason codes, lets explicit structured scopes execute only after schema validation, and logs non-sensitive source-selection diagnostics. Structured count/list planning now requires entity/filter support instead of generic row counts, and grounded answers are rejected if selected source, structured plan and citations do not use the same authorized document.
- Citation handling: structured aggregate sources now include `row_ranges` covering contributing rows, while preserving `row_start` and `row_end` for compatibility.
- Security and tenant isolation: source validation reuses authenticated owner/organization predicates, active current-version checks, soft-delete checks, processed workbook readiness, and indexed chunk readiness. Diagnostic events include IDs, scores, plan outcomes and reason codes only; no document contents, raw cell values, credentials, JWTs, API keys or embeddings are logged.
- Tests run: `.\venv\Scripts\python.exe -m unittest tests.test_source_selection_gate tests.test_workbook_domain_neutral tests.test_chat` -> 32 tests OK; `.\venv\Scripts\python.exe -m unittest tests.test_legacy_csv_reindex_retrieval.LegacyCsvReindexRetrievalTests.test_legacy_csv_becomes_deterministically_queryable_after_reindex` -> OK; `.\venv\Scripts\python.exe -m unittest discover -s tests` -> 144 tests OK; `.\venv\Scripts\python.exe -m compileall app tests` -> OK; `git diff --check` -> OK with CRLF normalization warnings only.
- Original failure-class reproduction: generated tests cover an unrelated structured workbook with weak headers and a relevant unstructured document; the workbook is rejected with no row-count answer, the unstructured source wins when evidence is sufficient, and unavailable/ambiguous answers return `grounded=false` with `sources=[]`.
- Remaining risks or limitations: document-wide totals from unstructured top-K retrieval are still not calculated deterministically; the system returns unavailable or asks for clarification unless complete structured evidence exists. Candidate ambiguity now prefers clarification over arbitrary tie-breaking.

## 2026-08-03 15:35:46 +05:30 - Generic PDF table structured analysis

- Confirmed root cause: PDF page text was chunked with whitespace splitting before table analysis, so usable row boundaries were lost. The PDF remained visible and vector-indexed, but metric questions had no structured rows to calculate from and could safely end as `Information not available in the uploaded files.` when retrieval evidence was insufficient.
- Files modified: `backend/app/services/workbooks.py`, `backend/app/services/ingestion_jobs.py`, `backend/app/routes/upload.py`, `backend/app/services/workbook_analysis.py`, `backend/app/services/source_selection.py`, and `backend/tests/test_workbook_domain_neutral.py`.
- Generic solution: clear text-extracted PDF tables are converted into the existing structured workbook row model before retrieval chunking removes line breaks. The async ingestion worker and legacy upload helper both persist those rows. The deterministic planner now handles formatted percentages, currency-like values, parenthesized negatives, repeated headers, ambiguous equal-score numeric columns, and PDF source citations with page and table metadata.
- Security and grounding impact: no similarity threshold was lowered. Existing owner, organization, current-version, soft-delete, and source-validation checks still gate structured analysis and citations. Ambiguous numeric targets return unavailable with `grounded=false` and `sources=[]` instead of guessing.
- Tests and exact results: `.\venv\Scripts\python.exe -m unittest tests.test_workbook_domain_neutral -v` passed 20 tests in 69.047 seconds; `.\venv\Scripts\python.exe -m unittest tests.test_chat tests.test_source_selection_gate tests.test_document_parsers -v` passed 27 tests in 1.917 seconds; `.\venv\Scripts\python.exe -m compileall app tests` passed; `.\venv\Scripts\python.exe -m unittest discover -s tests` passed 146 tests in 309.523 seconds. Frontend `npm run build` passed with Vite's existing non-fatal chunk-size warning.
- Database impact: no schema migration or production data change was made. The fix writes PDF table rows into the existing `workbook_sheets` and `workbook_rows` tables for newly processed documents.

## 2026-08-10 - Dedicated RAG evaluation test structure

- Task name: Dedicated RAG evaluation test structure.
- Affected files: `backend/tests/rag_eval/__init__.py`, `backend/tests/rag_eval/cases.json`, `backend/tests/rag_eval/helpers.py`, `backend/tests/rag_eval/test_eval_framework.py`, and `changes/changes_backend`.
- What changed: added a test-only RAG evaluation package with a JSON regression-case catalog, schema validation, category grouping, normalized retrieval observations, normalized generation observations, and reusable assertions that evaluate retrieval/routing separately from answer-generation behavior.
- Why: RAG correctness work needs reproducible cases that distinguish whether failures come from retrieval/source routing or from answer generation, without calling external evaluation services or changing production RAG logic.
- Validation performed: `.\venv\Scripts\python.exe -m unittest discover -s tests\rag_eval -v` passed 5 tests; `.\venv\Scripts\python.exe -m unittest tests.test_chat tests.test_source_selection_gate -v` passed 14 existing RAG policy/source-selection tests; `.\venv\Scripts\python.exe -m compileall tests\rag_eval` passed.
- Known limitations: the new framework validates case loading and reusable expectations but does not yet execute live app retrieval/generation against seeded documents; future regression suites should connect these helpers to controlled fixtures with stable document and chunk IDs.

## 2026-08-10 - Known RAG failure-pattern regression baseline

- Date: 2026-08-10 10:54:43 +05:30.
- Task name: Known RAG failure-pattern regression baseline.
- Affected files: `backend/tests/rag_eval/cases.json`, `backend/tests/rag_eval/regression_cases.json`, `backend/tests/rag_eval/regression_helpers.py`, `backend/tests/rag_eval/test_known_failure_regressions.py`, `backend/tests/rag_eval/test_eval_framework.py`, `backend/tests/rag_eval/BASELINE.md`, and `changes/changes_backend`.
- What changed: added 14 domain-neutral regression scenarios and generated 28 independently reported checks, split evenly between retrieval/routing behavior and final-answer behavior. The isolated harness reuses `backend/tests/fixtures/agriculture_dataset.csv`, builds small multi-tab and multi-workbook fixtures in memory, runs existing ingestion/structured-index paths, validates prior-turn facts and sources, enforces all required case IDs, and disables external answer generation. Starter cases now use synthetic names, filenames, and IDs.
- Why: preserve known project failure patterns as a reproducible baseline before any future production RAG changes, including multi-tab workbooks, exact names and values, counts, percentages, ranges, named sources, unavailable answers, bounded follow-ups, multiple workbooks, and ambiguous workbook selection.
- Validation performed: `\.\venv\Scripts\python.exe -m unittest tests.rag_eval.test_known_failure_regressions -v` ran 28 checks in 50.228 seconds: 27 passed and 1 intentionally recorded current failure. All 14 retrieval checks passed; 13 of 14 final-answer checks passed. `\.\venv\Scripts\python.exe -m unittest tests.rag_eval.test_eval_framework tests.test_chat tests.test_source_selection_gate tests.test_workbook_domain_neutral -v` passed all 51 existing/framework checks in 120.773 seconds. `\.\venv\Scripts\python.exe -m unittest tests.rag_eval.test_eval_framework tests.rag_eval.test_known_failure_regressions -v` confirmed 33 of 34 checks pass with only the recorded answer baseline failing. `\.\venv\Scripts\python.exe -m compileall tests\rag_eval` passed. Final read-only security and test-architecture reviews passed after test-only hardening.
- Known limitations: `test_answer__follow_up_show_those` currently fails because the follow-up remains scoped to the correct CSV but returns the matched price values (`125000`, `75000`, `200000`) instead of the matching equipment names (`Power Tiller`, `Irrigation Pump`, `Harvester`). Per task scope, no production fix was made. The suite uses controlled local fixtures rather than production documents or external evaluators.

## 2026-08-10 - Deterministic RAG retrieval metrics

- Date: 2026-08-10 11:16:29 +05:30.
- Task name: Deterministic RAG retrieval metrics.
- Affected files: `backend/tests/rag_eval/metrics.py`, `backend/tests/rag_eval/test_retrieval_metrics.py`, and `changes/changes_backend`.
- What changed: added lightweight, reusable calculations for Hit Rate@K, Recall@K, Precision@K, reciprocal rank and Mean Reciprocal Rank, correct-source selection, and expected-chunk rank. Metrics support default K values 1, 3, 5, 10, and 15 plus custom positive integer cutoffs.
- Why: provide deterministic retrieval-quality measurements inside the local RAG evaluation framework without changing production retrieval behavior or adding external evaluation dependencies.
- Validation performed: `.\venv\Scripts\python.exe -m unittest tests.rag_eval.test_retrieval_metrics tests.rag_eval.test_eval_framework -v` passed all 14 metric/framework tests in 0.004 seconds. Focused metric validation passed all 8 tests and covers relevant results at ranks 1 and 5, no relevant result, multiple relevant results, MRR, source/chunk helpers, configurable K values, duplicate K values, and invalid inputs. Read-only metric-correctness and security reviews passed; `git diff --check` reported no whitespace errors.
- Known limitations: metrics operate on supplied ranked identifiers and relevance labels; they do not create ground truth, run retrieval, or automatically aggregate the existing regression catalog. Precision@K uses K as its denominator even when fewer than K results are supplied.

## 2026-08-10 - Opt-in internal RAG request diagnostics

- Date: 2026-08-10 11:30:42 +05:30.
- Task name: Opt-in internal RAG request diagnostics.
- Affected files: `backend/app/services/rag_diagnostics.py`, `backend/app/services/rag_service.py`, `backend/app/services/source_selection.py`, `backend/tests/test_rag_diagnostics.py`, and `changes/changes_backend`.
- What changed: added an allowlisted in-memory diagnostic representation for a single RAG request and optional trace hooks in orchestration and source selection. It records routing decisions and reasons, safe scope filters, primary/fallback retrieval configuration and identifiers, source/chunk IDs and scores, structured-analysis path, final context chunk IDs, and grounded/unavailable status. Original and resolved-query schema fields are present but raw query text is intentionally omitted because free-form text cannot be reliably scrubbed of every credential or pasted private passage. Valid UUID conversation IDs remain readable; every non-UUID value is stored only as a SHA-256 fingerprint. No endpoint, persistence, or diagnostic logging was added.
- Why: provide deterministic internal evidence for diagnosing routing, retrieval, structured analysis, follow-up handling, and answer grounding without changing production retrieval behavior or exposing private document content and credentials.
- Validation performed: `\.\venv\Scripts\python.exe -m unittest tests.test_rag_diagnostics tests.test_chat tests.test_source_selection_gate -v` passed all 24 tests; `\.\venv\Scripts\python.exe -m compileall app tests\test_rag_diagnostics.py` passed; `git diff --check` passed with only existing CRLF normalization warnings. Read-only test, security, and documentation reviews were run through the project engineering workflow.
- Known limitations: diagnostics exist only when an internal caller explicitly passes a `RagRequestDiagnostic`; no current API route enables or exposes them. Original and resolved follow-up query text remain null pending a privacy-safe representation, and reranking scores remain null unless a retriever supplies them.

## 2026-08-10 - Protected RAG diagnostic endpoint

- Date: 2026-08-10 12:17:41 +05:30.
- Task name: Protected RAG diagnostic endpoint.
- Affected files: `backend/.env.example`, `backend/README.md`, `backend/app/config.py`, `backend/app/routes/chat.py`, `backend/app/services/rag_diagnostics.py`, `backend/app/services/rag_service.py`, `backend/tests/test_rag_diagnostic_endpoint.py`, `backend/tests/test_rag_diagnostics.py`, and `changes/changes_backend`.
- What changed: added authenticated `POST /chat/diagnostics`, guarded by the disabled-by-default `RAG_DIAGNOSTICS_ENABLED` environment setting and clearly tagged as a development diagnostic capability. It runs the existing RAG path with an in-memory trace, does not save diagnostic results as follow-up conversation context, and returns only routing, authorized source/chunk IDs, retrieval configuration and scores, structured-analysis path, final-context IDs, and grounding/unavailable metadata. Explicit document scopes are authorized before retrieval, and all traced document/chunk IDs are rechecked against organization, visibility, sharing, soft-deletion, and version boundaries before serialization. Query text, answers, filenames, source locations, document contents, previews, authentication data, configuration, and secrets are omitted. The standard `POST /chat` response remains unchanged.
- Why: provide protected retrieval troubleshooting without exposing private document data, crossing tenant or document ACL boundaries, mutating normal conversation context, or enabling diagnostics implicitly in production.
- Validation performed: the first endpoint run passed 4 checks and found one test-fixture schema mismatch (`granted_by` does not exist), which was corrected to the actual `document_permissions` schema. Read-only security/test reviews then identified verbatim diagnostic error details, rate limiting after ACL lookup, missing deleted-version reauthorization, and diagnostic context persistence; each received a minimal root-cause fix and regression coverage. `\.\venv\Scripts\python.exe -m unittest tests.test_rag_diagnostic_endpoint tests.test_rag_diagnostics tests.test_chat tests.test_source_selection_gate -v` passed all 33 tests; `\.\venv\Scripts\python.exe -m unittest tests.test_multitenant_architecture -v` passed all 15 tenant/lifecycle tests; `\.\venv\Scripts\python.exe -m compileall app tests\test_rag_diagnostic_endpoint.py tests\test_rag_diagnostics.py` passed; `git diff --check` passed with only existing CRLF normalization warnings. Final read-only test and security reviews passed with no remaining findings.
- Known limitations: diagnostics are request-local and not persisted; query and resolved follow-up text are deliberately omitted; chunk previews are always empty; reranking scores remain null without a reranker. Enabling the global flag permits every authenticated user to call the endpoint subject to their current organization/document ACLs. A diagnostic run uses the normal chat request allowance and may consume configured answer-provider quota because it executes the complete RAG request.

## 2026-08-10 - Bounded local embedding initialization

- Date: 2026-08-10.
- Task name: Investigate and bound direct semantic retrieval initialization.
- Affected files: `backend/app/services/embeddings.py`, `backend/app/config.py`, `backend/app/routes/health.py`, `backend/.env.example`, `backend/README.md`, `backend/tests/test_embeddings.py`, and `changes/changes_backend`.
- What changed: synchronized the process-local sentence-transformers loader, added configurable `EMBEDDING_MODEL_LOAD_TIMEOUT_SECONDS` (default 60), safe initialization status reporting, non-sensitive structured load/failure events, and non-blocking embedding state in liveness/readiness responses. Added tests for concurrent reuse, bounded waiting, and safe failure metadata.
- Why: direct concurrent semantic probes could all pass the unsynchronized `model is None` check and independently import/load the same local model, making the cold path appear to hang under contention.
- Validation performed: before change, cold `sentence_transformers` import measured 11.0334 s, full cold retrieval measured 13.6933 s, and six concurrent model callers created six instances. Local Qdrant measured 0.2252 s to open disk storage, 0.0006 s for collection access, and 0.0043 s for raw search; a second disk client failed fast in 0.0573 s. After change, six concurrent callers completed in 13.5429 s with one model instance and no errors; warm retrieval after initialization measured 0.2292 s. `venv\Scripts\python.exe -m unittest tests.test_embeddings tests.test_vector_store_singleton -v` passed 6 tests; `venv\Scripts\python.exe -m unittest tests.test_qdrant_integration.QdrantIntegrationTests.test_sqlite_feature_flag_provides_validated_rollback -v` passed, including `/health`; `venv\Scripts\python.exe -m compileall app tests/test_embeddings.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: the initial Python import/model construction remains synchronous and can legitimately take about 11-14 seconds on this machine; Python cannot safely cancel a thread currently constructing the model. The timeout bounds only additional callers waiting for that initialization. Local Qdrant mode remains unchanged and supported; no Docker or production retrieval behavior was introduced or changed.

## 2026-08-10 - Source-selection routing regression coverage

- Date: 2026-08-10.
- Task name: Comprehensive source-selection routing tests.
- Affected files: `backend/tests/test_source_selection_gate.py` and `changes/changes_backend`.
- What changed: expanded the isolated source-selection gate suite with stable mocked retrieval/planner seams. Coverage now asserts selected document, route path, routing reason, exposed candidate score, rejection reason, and version search filters for named files, schema-driven workbooks, exact column/value filters, multiple documents, weak numeric overlap, ambiguity, absent sources, semantic text, multi-workbook selection, and inaccessible/current version scopes.
- Why: protect the centralized routing gate against selecting a weak, stale, unauthorized, or ambiguous source while keeping routing behavior observable and testable without calling embedding/vector services.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_source_selection_gate tests.test_chat -v` passed 26 tests (15 source-selection cases and 11 existing chat/routing tests); `venv\Scripts\python.exe -m compileall app tests/test_source_selection_gate.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: the matrix tests source-selection decisions through deterministic planner/retrieval seams rather than a live vector index or production documents. Workbook parsing and vector ranking remain independently covered by their existing suites.

## 2026-08-10 - Safe deterministic follow-up context routing

- Date: 2026-08-10.
- Task name: Improve follow-up detection and context constraints.
- Affected files: `backend/app/services/chat_context.py`, `backend/app/config.py`, `backend/.env.example`, `backend/tests/test_chat_context.py`, and `changes/changes_backend`.
- What changed: made follow-up detection conservative for short deictic commands such as `name them`, `what about those`, `and the others`, and `show details`. Pronouns paired with a substantive new topic now continue through normal routing. Follow-ups require a conversation, a fresh grounded context within configurable `CHAT_FOLLOW_UP_CONTEXT_MINUTES` (default 30), and reusable structured rows or an employee-profile result; otherwise they return the normal unavailable response before retrieval or generation. The existing newest-context `ORDER BY id DESC` behavior remains unchanged.
- Why: the prior detector treated any pronoun as a follow-up, allowing stale or irrelevant context to constrain independent questions. Reference commands without valid context need a deterministic unavailable response rather than an unscoped global search.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_chat_context tests.test_chat -v` passed 18 tests; the added seven tests cover valid references, independent pronoun questions, freshness, missing conversation/context, and no-search/no-LLM behavior. The broader `tests.rag_eval.test_known_failure_regressions` run kept its one pre-existing `follow_up_show_those` answer-format baseline failure and introduced no additional failures. `venv\Scripts\python.exe -m compileall app tests/test_chat_context.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: reusable deterministic follow-ups currently require persisted workbook rows or employee profiles; a previous unstructured-only answer deliberately returns unavailable for a vague reference instead of attempting a new global search. The stored context TTL remains six hours for cleanup, while follow-up eligibility uses the shorter configurable freshness window.

## 2026-08-10 - Workbook extraction provenance audit

- Date: 2026-08-10.
- Task name: Focused workbook extraction audit and multi-sheet regression coverage.
- Affected files: `backend/app/services/source_extraction.py`, `backend/tests/test_workbook_extraction_audit.py`, and `changes/changes_backend`.
- What changed: audited `workbooks.py`, `source_extraction.py`, `workbook_sheets`, and `workbook_rows`. Added multi-sheet regression coverage for sheet names/boundaries, headers, source row locations, blank rows, vertically merged data labels, numeric values, date strings, and formula provenance. Fixed confirmed duplicate header ambiguity in both `.xlsx` and `.xls` source chunks by emitting stable labels such as `Amount` and `Amount (2)`, matching structured workbook rows.
- Why: duplicate labels in row-level source chunks made citations and retrieval context ambiguous even though structured storage already disambiguated them.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_workbook_extraction_audit tests.test_document_parsers tests.test_workbook_domain_neutral.WorkbookDomainNeutralTests.test_multi_sheet_tables_are_schema_indexed_and_counted -v` passed 16 tests; `venv\Scripts\python.exe -m compileall app/services/workbooks.py app/services/source_extraction.py tests/test_workbook_extraction_audit.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: formulas are not evaluated by the application. Structured extraction uses the workbook's cached/displayed value where available, while source chunks retain formula text and include a cached value when present. Date values are normalized to ISO strings for JSON-safe persistence, preserving identifiable date semantics but not an Excel-native date type.

## 2026-08-10 - Domain-neutral workbook question planning

- Date: 2026-08-10.
- Task name: Generic workbook-analysis classification.
- Affected files: `backend/app/services/workbook_analysis.py`, `backend/app/services/chat_context.py`, `backend/tests/test_workbook_analysis_generic.py`, `backend/tests/test_chat_context.py`, and `changes/changes_backend`.
- What changed: removed commercial-column-name gating from numeric selection, so any column with usable numeric values can support an evidence-matched aggregation or range. Replaced attendance-header-specific IN/OUT inference with a unique matching value-column rule. Added a deterministic `comparison` plan for `compare`/`versus` questions rather than treating them as arbitrary lists. Added synthetic fixture-independent coverage for lookup, filters, counts, aggregation, percentage classification, ranges, comparisons, lists, grouping, and generic IN/OUT values. A narrow punctuation-safe named profile continuation guard was added to preserve an existing grounded employee-profile follow-up after the earlier context-conservatism change.
- Why: planning should be based on question tokens, column/schema evidence, and actual values across HR, finance, cost, inventory, project, sales, attendance, ratings, and operational tables, not a fixed business vocabulary or fixture filename.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_workbook_analysis_generic tests.test_workbook_domain_neutral -v` passed 38 tests in 38.241 seconds; targeted generic-planning, context, and employee follow-up rerun passed 16 tests; `venv\Scripts\python.exe -m compileall app/services/workbook_analysis.py app/services/chat_context.py tests/test_workbook_analysis_generic.py tests/test_chat_context.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: percentage/rate questions continue to use the existing average aggregation because a generic percentage denominator cannot be inferred safely. The isolated employee-profile formatter remains as a compatibility adapter for form-style sheets; normal table planning does not rely on its business nouns.

## 2026-08-10 - Deterministic structured numeric ranges

- Date: 2026-08-10.
- Task name: Make spreadsheet range queries deterministic.
- Affected files: `backend/app/services/workbook_analysis.py`, `backend/tests/test_workbook_analysis_generic.py`, and `changes/changes_backend`.
- What changed: added `from X to Y` as an inclusive structured range equivalent to `between X and Y`; documented and regression-tested exclusive `greater than`/`above` and `less than`/`below`, plus inclusive `at least`/`at most`. Percentage-point query bounds now convert to fractional bounds only when the selected column's observed values are fractional. Currency-formatted numeric strings and decimal comparisons continue to use `Decimal`, avoiding float conversion.
- Why: a recognized analytical phrase must produce the same deterministic row filter across spreadsheets, including fraction-stored percentages and formatted currency, rather than falling through to a non-range plan.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_workbook_analysis_generic tests.test_workbook_domain_neutral -v` passed 41 tests in 32.165 seconds. The new regressions cover all requested phrases, endpoint inclusion/exclusion, percentage fractions, currency strings, and decimal precision. `venv\Scripts\python.exe -m compileall app/services/workbook_analysis.py tests/test_workbook_analysis_generic.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: percentage scaling is applied only to explicit percentage questions and columns whose observed numeric values are all at most one in magnitude; columns represented as percentage points retain their existing scale. The system intentionally does not infer unsupported currency words or evaluate formulas during range filtering.

## 2026-08-10 - Complete structured workbook aggregation evaluation

- Date: 2026-08-10.
- Task name: Extend structured workbook evaluation for aggregation questions.
- Affected files: `backend/app/services/workbook_analysis.py`, `backend/tests/test_workbook_analysis_generic.py`, `backend/tests/test_workbook_domain_neutral.py`, and `changes/changes_backend`.
- What changed: added a complete-row, multi-tab aggregation regression matrix covering count, distinct count, sum, average, percentage/rate average, minimum, maximum, grouped sum, selected-sheet filtering, and document selection. Added a generic `-ies` singular normalization so plural schema references such as `categories` bind to a `Category` column. The end-to-end structured path test raises if vector search or LLM answer generation is invoked.
- Why: structured arithmetic must operate over all authorized rows in the selected current workbook scope, rather than a top-K retrieval sample, while retaining document, ACL, soft-deletion, and current-version boundaries.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.test_workbook_analysis_generic tests.test_workbook_domain_neutral -v` passed 44 tests in 38.586 seconds; the focused aggregation subset passed 13 tests. `venv\\Scripts\\python.exe -m compileall app/services/workbook_analysis.py tests/test_workbook_analysis_generic.py tests/test_workbook_domain_neutral.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: percentage/rate questions retain the existing average-based interpretation because a generic numerator and denominator cannot be inferred safely. Aggregations deliberately select one authorized, current document scope; combining separate workbooks requires an explicit future cross-document policy.

## 2026-08-10 - Structured workbook provenance and follow-up plan validation

- Date: 2026-08-10.
- Task name: Improve structured workbook answer provenance.
- Affected files: `backend/app/services/workbook_analysis.py`, `backend/app/services/source_selection.py`, `backend/app/services/chat_context.py`, `backend/tests/test_source_selection_gate.py`, `backend/tests/test_workbook_domain_neutral.py`, and `changes/changes_backend`.
- What changed: structured answers now return compact provenance containing document and version IDs, workbook filename, worksheet row ranges, columns used, applied filters, numeric filter, aggregation/operation, and contributing-row count. The same object is retained as the server-side result plan. Result validation rejects provenance whose document, version, worksheet, or row range falls outside cited accessible sources; follow-up context persistence and reuse reject malformed row references.
- Why: calculations and follow-up answers need an explainable, authorization-safe plan without returning worksheet row values or allowing a mismatched/stale source plan to be reused.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.test_source_selection_gate tests.test_workbook_domain_neutral -v` passed 51 tests in 85.044 seconds. New coverage verifies compact aggregation provenance, source/result-plan range mismatch rejection, and invalid provenance row references not being persisted for follow-up. `venv\\Scripts\\python.exe -m compileall app/services/workbook_analysis.py app/services/source_selection.py app/services/chat_context.py tests/test_source_selection_gate.py tests/test_workbook_domain_neutral.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: provenance describes the final eligible row ranges, not individual cell values or formula lineage. Existing contexts created before this change have no structured result plan and are conservatively unavailable for generic row-based follow-ups.

## 2026-08-10 - all-MiniLM vector retrieval identifier baseline

- Date: 2026-08-10.
- Task name: Create vector retrieval tests before hybrid search.
- Affected files: `backend/tests/rag_eval/test_minilm_vector_baseline.py` and `changes/changes_backend`.
- What changed: added an isolated, synthetic, local all-MiniLM-L6-v2 dense-retrieval baseline. It uses same-template near-duplicate chunks for employee names, employee IDs, codes, filenames, exact labels, numbers, column headings, and unusual abbreviations, then reports Recall@5, Recall@10, and the correct-chunk rank without an external evaluation service or production data.
- Why: establish which exact-reference query classes need a hybrid-search decision before changing retrieval behavior.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.rag_eval.test_retrieval_metrics tests.rag_eval.test_minilm_vector_baseline -v` passed 9 tests in 21.736 seconds; `venv\\Scripts\\python.exe -m compileall tests/rag_eval/test_minilm_vector_baseline.py` passed; `git diff --check` passed with existing CRLF normalization warnings only. Measured results: employee name, ID, code, exact label, column heading, and unusual abbreviation were Recall@5=1/Recall@10=1/rank 1; number was 1/1/rank 3; filename was 0/1/rank 9.
- Known limitations: this is a controlled 72-chunk synthetic near-duplicate corpus, not a production-data benchmark. The filename failure measures dense vector search only; the existing source-selection filename router can scope a document before retrieval. Scores and ranks may change if the embedding model, chunking, or vector provider changes.

## 2026-08-10 - Lightweight local hybrid retrieval

- Date: 2026-08-10.
- Task name: Add keyword/BM25 retrieval alongside vector search.
- Affected files: `backend/app/services/keyword_search.py`, `backend/app/services/vector_search.py`, `backend/tests/test_keyword_search.py`, `backend/tests/rag_eval/test_minilm_vector_baseline.py`, and `changes/changes_backend`.
- What changed: added a dependency-free local BM25-style lexical scorer over authorized SQLite chunk text, filenames, and source-location metadata. `search_chunks` retains the existing vector-store search and merges independent vector and keyword rankings with reciprocal-rank fusion, exposing separate `vector_score` and `keyword_score` when present. The lexical query uses the same organization, readable-document ACL, current-version, completed-version, and soft-delete predicates as vector search.
- Why: Task 15 demonstrated that vector-only filename lookup missed Recall@5; exact identifiers and filenames need a local lexical signal without replacing Qdrant or sending content externally.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.test_keyword_search tests.test_document_duplicates tests.rag_eval.test_retrieval_metrics tests.rag_eval.test_minilm_vector_baseline -v` passed 29 tests in 56.803 seconds; the final `tests.test_keyword_search tests.test_qdrant_integration.QdrantIntegrationTests.test_no_relevant_result_returns_safe_message tests.rag_eval.test_minilm_vector_baseline -v` rerun passed 4 tests in 27.725 seconds. `venv\\Scripts\\python.exe -m compileall app/services/keyword_search.py app/services/vector_search.py tests/test_keyword_search.py tests/rag_eval/test_minilm_vector_baseline.py` passed; `git diff --check` passed with existing CRLF normalization warnings only. On the 72-chunk baseline, filename retrieval improved from vector-only Recall@5=0/Recall@10=1/rank 9 to hybrid Recall@5=1/Recall@10=1/rank 5; numeric retrieval improved from rank 3 to rank 1, while all other measured classes remained rank 1.
- Known limitations: BM25 scoring currently scans the authorized local chunk set in process; it is intentionally lightweight but should be profiled before very large corpus deployments. Fusion only improves retrieval ranking; it does not alter source-selection ACL checks or structured-workbook routing.

## 2026-08-10 - Configurable deterministic vector and keyword fusion

- Date: 2026-08-10.
- Task name: Complete configurable reciprocal-rank fusion retrieval.
- Affected files: `backend/app/config.py`, `backend/.env.example`, `backend/app/services/vector_search.py`, `backend/app/services/rag_diagnostics.py`, `backend/tests/rag_eval/test_minilm_vector_baseline.py`, `backend/tests/test_rag_diagnostics.py`, and `changes/changes_backend`.
- What changed: made hybrid retrieval configuration-driven with `RAG_RETRIEVAL_MODE` (`hybrid` or vector-only rollback), independent vector and keyword candidate limits, and configurable `RAG_RRF_K`. Reciprocal-rank fusion is deterministic, deduplicates by chunk ID, retains complete source metadata, and emits aligned vector, keyword, and fusion scores through the content-free diagnostic trace. Vector-only mode bypasses keyword retrieval entirely.
- Why: make the improved exact-term retrieval behavior safely observable, tunable, and immediately reversible without removing Qdrant or introducing LLM-based fusion.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.rag_eval.test_retrieval_metrics tests.rag_eval.test_minilm_vector_baseline tests.test_keyword_search tests.test_rag_diagnostics tests.test_qdrant_integration.QdrantIntegrationTests.test_no_relevant_result_returns_safe_message -v` passed 23 tests in 25.005 seconds. The baseline asserts hybrid Recall@5 and Recall@10 never decline by query class; vector-only filename retrieval remains Recall@5=0/rank 9, while hybrid is Recall@5=1/rank 5. `venv\\Scripts\\python.exe -m compileall app/config.py app/services/keyword_search.py app/services/vector_search.py app/services/rag_diagnostics.py tests/rag_eval/test_minilm_vector_baseline.py tests/test_keyword_search.py tests/test_rag_diagnostics.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: the evaluation gate is a synthetic 72-chunk corpus and does not replace a production-scale benchmark. Keyword ranking remains local query-time scoring, and the configured candidate limits should be profiled before use with very large authorized corpora.

## 2026-08-10 - RAG minimum-score distribution evaluation

- Date: 2026-08-10.
- Task name: Evaluate `RAG_MIN_SCORE` weak-evidence threshold.
- Affected files: `backend/app/config.py`, `backend/.env.example`, `backend/tests/rag_eval/test_minilm_vector_baseline.py`, `backend/tests/test_source_selection_gate.py`, and `changes/changes_backend`.
- What changed: added a raw vector-score distribution probe for relevant, exact lookup, broad, irrelevant, and unanswerable queries before threshold filtering. Added an unavailable-routing regression for below-floor, unmatched retrieval evidence. Aligned the fallback setting and example environment value to the already active local `RAG_MIN_SCORE=0.30`; the setting remains environment-driven.
- Why: determine whether the threshold should be globally tuned or treated only as a weak-vector-evidence guard after hybrid retrieval was introduced.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.rag_eval.test_minilm_vector_baseline tests.test_source_selection_gate tests.test_qdrant_integration.QdrantIntegrationTests.test_no_relevant_result_returns_safe_message -v` passed 21 tests in 35.477 seconds; `venv\\Scripts\\python.exe -m compileall app/config.py app/services/vector_search.py tests/rag_eval/test_minilm_vector_baseline.py tests/test_source_selection_gate.py` passed; `git diff --check` passed with existing CRLF normalization warnings only. At 0.30, relevant/exact/broad correct-chunk scores were 0.5662/0.5805/0.4935; irrelevant/unanswerable top scores were 0.1121/0.2318. Exact and broad queries still had 11 and 10 near-duplicate candidates above threshold, respectively.
- Known limitations: this local all-MiniLM-L6-v2 measurement is a synthetic 72-chunk corpus. The evidence supports one global 0.30 threshold only as a weak-evidence floor, not as a complete relevance classifier; no query-type-specific vector threshold is warranted from this baseline. Production corpus distributions should be profiled separately before further threshold changes.

## 2026-08-10 - Hybrid retrieval ranking quality evaluation

- Date: 2026-08-10.
- Task name: Evaluate whether retrieval ranking warrants a reranker.
- Affected files: `backend/tests/rag_eval/test_minilm_vector_baseline.py` and `changes/changes_backend`.
- What changed: added a rank-focused evaluation over the local exact-reference corpus plus a uniquely attributable broad query. It reports correct-chunk rank, MRR, and average Precision@5 for vector-only and hybrid retrieval. No reranker or production retrieval behavior was added.
- Why: distinguish retrieval-recall problems from a correct-but-too-low ranking problem before adding a reranker.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.rag_eval.test_retrieval_metrics tests.rag_eval.test_minilm_vector_baseline -v` passed 12 tests in 40.812 seconds; `venv\\Scripts\\python.exe -m compileall tests/rag_eval/test_minilm_vector_baseline.py` passed; `git diff --check` passed with existing CRLF normalization warnings only. Vector-only MRR was 0.8272 and Precision@5 was 0.1778, with filename rank 9 and numeric rank 3. Hybrid MRR was 0.9111 and Precision@5 was 0.2000; filename rank improved to 5, numeric rank to 1, and all nine labeled correct chunks were within the top five.
- Known limitations: the one-relevant-chunk-per-query setup caps Precision@5 at 0.20, so its absolute value should be interpreted alongside MRR and rank. This is a synthetic corpus, not a production ranking benchmark.

## 2026-08-10 - Evidence-aware final RAG context selection

- Date: 2026-08-10.
- Task name: Replace blind final top-five context selection with bounded evidence-aware selection.
- Affected files: `backend/app/services/rag_service.py`, `backend/app/services/vector_search.py`, `backend/app/services/keyword_search.py`, `backend/app/config.py`, `backend/.env.example`, `backend/tests/test_final_context_selection.py`, and `changes/changes_backend`.
- What changed: added a deterministic final-context selector that ranks by available fusion/relevance score, keeps a single-fact request to one best chunk, and expands complex requests up to the existing configured chunk cap only when additional evidence is useful. It first diversifies across documents, then uses adjacent chunk indexes, and suppresses normalized duplicate content. Added `RAG_FINAL_CONTEXT_TOKEN_BUDGET` (default 6000) with a conservative dependency-free estimate that prevents selected context from exceeding the hard budget. Vector and keyword retrieval now retain chunk indexes so real retrieval results can support adjacency.
- Why: a fixed top-five slice could crowd the answer prompt with repeated or single-source context while still omitting supporting evidence needed for comparisons and multi-part questions.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.test_final_context_selection tests.test_keyword_search -v` passed 8 tests in 4.811 seconds, including single fact, comparison, multi-document, multi-part, duplicate suppression, and hard-budget cases. `venv\\Scripts\\python.exe -m compileall app/services/rag_service.py app/services/vector_search.py app/services/keyword_search.py tests/test_final_context_selection.py` passed; `git diff --check` passed with existing CRLF normalization warnings only. A wider follow-up/diagnostics check ran 24 tests: 23 passed; pre-existing `tests.test_chat_context.FollowUpResolutionTests.test_valid_reference_uses_recent_grounded_rows` failed because no grounded result was returned, unrelated to final-context selection.
- Known limitations: token counting is a conservative character-based estimate rather than model-tokenizer exact accounting. Complexity detection is intentionally deterministic and compact; it expands for explicit comparison/cross-source language or multi-part `and` questions, while retrieval/source selection remain responsible for authorization and relevance.

## 2026-08-10 - Provenance-safe final context duplicate suppression

- Date: 2026-08-10.
- Task name: Suppress duplicate and overlapping retrieval evidence during final context assembly.
- Affected files: `backend/app/services/rag_service.py`, `backend/app/services/source_selection.py`, `backend/app/services/vector_search.py`, `backend/app/services/keyword_search.py`, `backend/tests/test_final_context_selection.py`, and `changes/changes_backend`.
- What changed: final context assembly now suppresses the same chunk, identical and near-identical prose from the same document, and repeated references that share the same canonical processed `content_id`. Candidate handoff no longer applies the final-context cap before evidence assembly, so a duplicate cannot consume an early slot and hide later useful evidence. Retrieval keeps `content_id` internally for this comparison; public citations retain their existing document, version, filename, source type, and location fields without exposing `content_id`. Workbook/CSV rows with different sheet-row provenance are deliberately retained even when their text is similar.
- Why: overlapping chunks and duplicate document references could flood the model with the same evidence, while aggressive text-only deduplication would risk removing distinct spreadsheet rows and their citations.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.test_final_context_selection tests.test_source_selection_gate tests.test_keyword_search -v` passed 29 tests in 5.452 seconds. Coverage includes exact duplicates, near-identical prose, overlapping prose with an appended tail, canonical-content duplicates across document references, and similar but distinct workbook rows. `venv\\Scripts\\python.exe -m compileall app/services/rag_service.py app/services/source_selection.py app/services/vector_search.py app/services/keyword_search.py tests/test_final_context_selection.py` passed; `git diff --check` passed with existing CRLF normalization warnings only. Static security review verified no ACL, organization, version, or soft-delete predicates changed; added metadata comes only from already authorized chunk rows.
- Known limitations: prose similarity is intentionally conservative (substantial token overlap or near containment, same document/canonical content only) and does not use a semantic model. Different documents with merely similar language remain available as independent comparison evidence unless they explicitly share canonical processed content.

## 2026-08-10 - Bounded current-version context neighbor expansion

- Date: 2026-08-10.
- Task name: Add useful adjacent chunk expansion for final RAG context.
- Affected files: `backend/app/config.py`, `backend/.env.example`, `backend/app/services/rag_service.py`, `backend/tests/test_final_context_selection.py`, and `changes/changes_backend`.
- What changed: added opt-in-by-threshold, deterministic expansion from high-relevance final context anchors to their immediate predecessor/successor chunks. Neighbor lookup is limited to the same document, canonical content identity, and current completed document version, runs through the existing organization/document ACL predicate, and requires a matching page, slide, sheet, or continuous text source. Expansion is capped by `RAG_NEIGHBOR_EXPANSION_MAX_NEIGHBORS` (default 2) and the existing final-context token budget. `RAG_NEIGHBOR_EXPANSION_MIN_SCORE` defaults to 0.50.
- Why: retrieval can correctly identify a heading or first part of an answer while its immediate body/table continuation is adjacent but not independently ranked highly enough to appear in the final prompt.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.test_final_context_selection tests.test_source_selection_gate tests.test_keyword_search -v` passed 32 tests in 5.419 seconds. Added coverage verifies same-section expansion, low-relevance and cross-document rejection, and hard token-budget enforcement. `venv\\Scripts\\python.exe -m compileall app/config.py app/services/rag_service.py tests/test_final_context_selection.py` passed; `git diff --check` passed with existing CRLF normalization warnings only. `venv\\Scripts\\python.exe -m unittest discover -s tests/rag_eval -p 'test_*.py' -v` measured unchanged retrieval metrics (hybrid MRR 0.9111 and Precision@5 0.2000); 45 of 46 evaluation tests passed. The pre-existing answer baseline `test_answer__follow_up_show_those` failed because its response omitted the expected `power tiller` phrase; neighbor expansion does not run for follow-up resolution and it was not changed in this task.
- Known limitations: neighbor expansion is context-only and the current evaluation suite measures retrieval/answer expectations rather than model-answer quality from expanded prompts. It intentionally considers only immediately adjacent chunks, so longer multi-chunk continuations require a separately retrieved anchor or a future explicitly scoped extension.

## 2026-08-10 - Structured table extraction across document formats

- Date: 2026-08-10.
- Task name: Preserve table headers, rows, and provenance in PDF, DOCX, XLSX, and PPTX source chunks.
- Affected files: `backend/app/services/source_extraction.py`, `backend/tests/test_document_parsers.py`, and `changes/changes_backend`.
- What changed: confirmed that XLSX already emits header-labeled row chunks with sheet, cell range, table, formula, and header provenance. Updated DOCX and PPTX table extraction to emit compact header/value row records rather than tab-separated values without column meaning. Added layout-preserved PDF table recognition: complete table rows include headers, page, table number, and original line-row provenance, and recognized table lines are excluded from generic prose chunking so table values cannot be split away from their headers.
- Why: table values without their associated columns are ambiguous during retrieval and cannot reliably support grounded answers or citations.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.test_document_parsers tests.test_workbook_extraction_audit -v` passed 17 tests in 3.162 seconds, including new DOCX/PPTX/PDF header and provenance coverage plus existing XLSX multi-sheet/table coverage. `venv\\Scripts\\python.exe -m unittest tests.test_workbook_domain_neutral tests.test_legacy_csv_reindex_retrieval -v` passed 36 tests in 54.376 seconds, including existing structured PDF-table calculations. `venv\\Scripts\\python.exe -m compileall app/services/source_extraction.py tests/test_document_parsers.py` passed.
- Known limitations: PDF table detection is deliberately limited to layout-preserved rows with consistent columns (pipes, tabs, or clear spacing); scanned PDFs still depend on OCR quality. Existing uploads retain stored chunks and require reprocessing/re-upload to receive the new table representation. No schema migration is required.

## 2026-08-10 - Parent-child retrieval evidence assessment

- Date: 2026-08-10.
- Task name: Assess small child retrieval chunks with parent context before implementation.
- Affected files: `changes/changes_backend` only.
- What changed: performed an investigation-only review of the current extraction, chunking, ingestion, storage, and RAG evaluation design. No production code, schema, retrieval behavior, or fixtures were changed.
- Why: determine whether parent-child retrieval addresses an observed failure before adding a storage and reindexing cost.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.rag_eval.test_known_failure_regressions -v` ran 28 checks in 48.828 seconds: all 14 retrieval checks passed, while 13 of 14 answer checks passed. The sole failure, `test_answer__follow_up_show_those`, retained the correct CSV source but returned price values rather than the matching equipment names. It is a structured follow-up result-formatting/provenance issue, not a missed chunk, low-ranked chunk, or insufficient parent context. Existing code inspection confirms generic prose is chunked at 800 words with 100-word overlap; structured workbook/PDF data uses row-level chunks and structured tables; chunks currently persist one text/vector record per source chunk with no parent identity.
- Known limitations: parent-child retrieval is not recommended from the present evidence. It could be applicable later to long TXT/DOCX/PDF/PPTX narrative sections or large tables whose header/body spans more than the current bounded adjacent-neighbor expansion, but not to XLSX/CSV structured rows or current aggregation paths. A typical future 200-word child / 50-word overlap design under 800-word parents would add roughly five child vectors per existing full parent and about 2.25x stored text before metadata, plus a parent mapping/table or columns and a full re-extraction/re-embedding/reindex. Expected measured accuracy improvement is currently 0 because the evaluation suite has no retrieval failure attributable to chunk granularity; add a long-section cross-boundary benchmark before reconsidering implementation.

## 2026-08-10 - Pre-generation grounded-evidence gate

- Date: 2026-08-10.
- Task name: Require sufficient retrieval evidence before producing a grounded answer.
- Affected files: `backend/app/services/rag_service.py`, `backend/tests/test_chat.py`, and `changes/changes_backend`.
- What changed: added a deterministic pre-generation gate after final context construction. It requires available final sources, a selected document, stable document/version/filename/location citation metadata, non-empty context, and either source-selection semantic confidence at or above the configured retrieval floor or direct query/context token overlap. Structured answers remain deterministic and continue to return only after existing citation/result-plan validation succeeds. Insufficient evidence returns the existing `UNAVAILABLE_ANSWER` with `grounded: false` and `sources: []`, without invoking the model or attaching citations.
- Why: model output alone must not turn weak, unrelated, empty, or uncitable retrieval context into a grounded response.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.test_chat tests.test_source_selection_gate tests.rag_eval.test_known_failure_regressions -v` ran 58 checks in 49.466 seconds: 57 passed. New chat regressions prove irrelevant low-confidence context and missing-version provenance do not call answer generation and return the standard unavailable shape. The sole existing failure is `test_answer__follow_up_show_those`, which returns structured price values rather than expected equipment names after follow-up resolution; this gate is not used for follow-ups and did not change it. `venv\\Scripts\\python.exe -m compileall app/services/rag_service.py tests/test_chat.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: the gate deliberately treats the already-selected retrieval score as a semantic-evidence signal because legitimate semantic matches may not share literal query terms. It does not interpret model confidence, generate explanations for unavailable responses, or replace the existing post-generation citation validation.

## 2026-08-10 - Final-context citation validation

- Date: 2026-08-10.
- Task name: Tighten citation validation without LLM sentence-level verification.
- Affected files: `backend/app/services/source_selection.py`, `backend/app/services/rag_service.py`, `backend/tests/test_source_selection_gate.py`, and `changes/changes_backend`.
- What changed: retrieval-answer validation now receives the final context allowlist and requires every returned citation to match a document ID, current version ID, filename, source type, source location, and source text that actually reached the answer model. It rejects missing version metadata, then retains the existing current-version/readability/organization ACL check. Any unavailable result carrying sources is normalized to the project unavailable response with an empty source list. Structured-result plan/provenance validation remains unchanged.
- Why: document-level agreement alone could not prove that a returned retrieval citation was one of the bounded final-context chunks rather than a discarded candidate.
- Validation performed: `venv\\Scripts\\python.exe -m unittest tests.test_source_selection_gate tests.test_chat tests.test_rag_diagnostics -v` passed 44 tests in 0.114 seconds. New coverage rejects a discarded-chunk citation, a missing-version citation, an unreadable/noncurrent citation, and evidence citations attached to an unavailable result. `venv\\Scripts\\python.exe -m compileall app/services/source_selection.py app/services/rag_service.py tests/test_source_selection_gate.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: validation verifies provenance and final-context membership, not sentence-level entailment of every generated claim. No LLM verifier was added because it would introduce cost, latency, and another probabilistic component without evidence that the existing deterministic grounding gate is insufficient.

## 2026-08-10 - Adversarial retrieved-document instruction regression tests

- Date: 2026-08-10.
- Task name: Verify RAG handling of prompt-injection text in indexed documents.
- Affected files: `backend/tests/test_rag_prompt_injection.py`, `backend/tests/test_keyword_search.py`, and `changes/changes_backend`.
- What changed: added synthetic malicious-document coverage for instruction override, system-prompt extraction, placeholder-secret disclosure, administrator impersonation, cross-user retrieval, and citation suppression language. The tests assert that the existing system and request prompts keep document content explicitly untrusted, instruction text remains inside the untrusted-context boundary, an ordinary grounded answer does not return the synthetic placeholder, and application-generated citations remain present. Added a normal ingestion/search ACL regression proving a second user cannot retrieve a private document merely because it contains adversarial text.
- Why: indirect prompt injection through indexed text must not alter RAG instructions, bypass access controls, remove provenance, or disclose sensitive data.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_rag_prompt_injection tests.test_keyword_search tests.test_chat -v` passed all 18 tests in 7.620 seconds, including the new untrusted-context, synthetic-placeholder, citation-preservation, and cross-user ACL cases. `venv\Scripts\python.exe -m compileall tests/test_rag_prompt_injection.py tests/test_keyword_search.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: the prompt-boundary test uses a deterministic answer-generation mock, so it verifies application prompt construction and provenance enforcement rather than claiming model-provider jailbreak resistance. The application does not store or use real secrets in these tests.

## 2026-08-10 - Vector-store current-point health statistics

- Date: 2026-08-10.
- Task name: Separate total, current, and stale vector-store health counts.
- Affected files: `backend/app/services/vector_store.py`, `backend/app/routes/health.py`, `backend/tests/test_vector_store_statistics.py`, `backend/tests/test_qdrant_integration.py`, and `changes/changes_backend`.
- What changed: added privacy-safe vector-store reconciliation statistics to `GET /health/ready`: `total_points`, `active_points`, `deleted_or_stale_points`, `sqlite_current_chunks`, and `sync_status`. Active points are now the intersection of provider points marked non-deleted and SQLite chunks belonging to a current, completed, non-deleted document version. `total_points` retains all persisted provider points; the difference is accurately classified as deleted or stale for operational reporting. The existing `points_count` field remains for compatibility. SQLite rollback health now also reports an explicit total.
- Why: Qdrant collection totals include retained historical vectors, so a large collection count must not be presented as the number of current searchable documents.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_vector_store_statistics tests.test_qdrant_integration.QdrantIntegrationTests.test_readiness_distinguishes_total_and_current_vector_points tests.test_qdrant_integration.QdrantIntegrationTests.test_qdrant_collection_created tests.test_qdrant_vector_store -v` passed all 4 tests in 5.884 seconds. Coverage verifies out-of-sync reconciliation and no content fields in statistics, plus an end-to-end soft-delete case where three total Qdrant points become zero active/current points and three deleted-or-stale points. The related legacy `/health` assertion was updated to include the already-existing embedding health field.
- Known limitations: reconciliation intentionally scrolls active provider point identifiers and compares them with SQLite identifiers, so it is an operational readiness statistic rather than a constant-time metric; it exposes only aggregate counts and no document, chunk, filename, or text data. `sync_status: in_sync` means the current SQLite and non-deleted provider ID sets agree, even when retained deleted/stale points remain and are reported separately.

## 2026-08-10 - Read-only SQLite/Qdrant discrepancy report

- Date: 2026-08-10.
- Task name: Diagnose active/current SQLite chunk and Qdrant vector consistency.
- Affected files: `backend/scripts/check_vector_consistency.py`, `backend/tests/test_vector_consistency.py`, and `changes/changes_backend`.
- What changed: extended the existing read-only vector consistency script with a structured `discrepancies` report. It now identifies current SQLite chunks without a vector ID, current SQLite vector IDs absent from active Qdrant points, active vectors without a valid current SQLite chunk, active vectors tied to deleted documents, wrong current-version or payload-version vectors, and duplicate active SQLite vector IDs. Existing summary/count and missing/unexpected ID fields remain available for compatibility. The report includes only vector, document, version, and chunk identifiers; it never selects or returns chunk/document text.
- Why: operational counts alone cannot explain whether a mismatch is an indexing gap, stale version, soft-delete lifecycle failure, or invalid provider metadata.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_vector_consistency tests.test_qdrant_integration.QdrantIntegrationTests.test_upload_indexes_chunks tests.test_qdrant_integration.QdrantIntegrationTests.test_readiness_distinguishes_total_and_current_vector_points -v` passed all 3 tests in 4.829 seconds. Unit coverage exercises every requested discrepancy category with synthetic identifiers; integration coverage verifies a healthy real Qdrant/SQLite reconciliation and soft-deletion behavior.
- Known limitations: Qdrant point IDs are unique by provider design, so duplicate active IDs are normally prevented by both Qdrant and SQLite constraints; the diagnostic still reports them defensively if corrupted/imported SQLite metadata is observed. The checker is deliberately read-only and does not repair, delete, or reindex any data.

## 2026-08-10 - Explicit-confirmation vector consistency repair command

- Date: 2026-08-10.
- Task name: Safely reindex current vectors and verify SQLite/Qdrant consistency.
- Affected files: `backend/scripts/repair_vector_consistency.py`, `backend/tests/test_repair_vector_consistency.py`, and `changes/changes_backend`.
- What changed: added `python -m scripts.repair_vector_consistency`, a dry-run-by-default operational command. Its read-only plan runs the existing current-version migration planner and consistency checker. Retrieval-affecting writes require both `--apply` and `--confirm-repair`; a confirmed run reuses the existing stable-ID, batch-verified migration, then runs final SQLite/Qdrant consistency verification and exits non-zero if discrepancies remain. Output and logs contain organization identifiers and counts only, while the checker retains identifier-only details without document text.
- Why: operators need a deliberate, restart-safe way to restore active/current vector consistency after interrupted indexing without running repair at application startup or deleting unknown data.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_repair_vector_consistency tests.test_vector_migration tests.test_vector_consistency -v` passed all 7 tests in 2.834 seconds. Coverage verifies default dry-run planning, no write attempt without explicit confirmation, confirmed reindex plus post-repair verification, stable-ID rerun after partial batch interruption, preservation of rollback embeddings, and discrepancy classification.
- Known limitations: this command intentionally does not delete unknown/orphaned Qdrant points. It repairs current eligible chunks through idempotent upserts and reports any remaining discrepancy for operator review. It is never invoked from production startup; a confirmed run may regenerate embeddings for current chunks whose retained SQLite embeddings are missing or incompatible.

## 2026-08-10 - all-MiniLM-L6-v2 retrieval category benchmark

- Date: 2026-08-10.
- Task name: Benchmark the configured local embedding model by query category.
- Affected files: `backend/tests/rag_eval/test_minilm_vector_baseline.py`, `backend/tests/rag_eval/MINILM_CATEGORY_BENCHMARK.md`, and `changes/changes_backend`.
- What changed: added a vector-only local evaluation with two synthetic probes each for semantic paraphrase, names, numbers, workbook labels, document headings, abbreviations, and cross-domain terminology. It calculates and prints Recall@5, Recall@10, MRR, and per-probe ranks using the existing deterministic metrics helpers. Added a checked-in evidence report alongside the pre-existing near-duplicate identifier stress corpus. No production embedding model, retrieval mode, data, or external service changed.
- Why: distinguish the configured dense model's semantic performance from its known susceptibility to exact identifier ranking errors before considering any model change.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.rag_eval.test_minilm_vector_baseline.MiniLMVectorRetrievalBaselineTests.test_all_minilm_category_benchmark -v` passed in 26.126 seconds: every clean-corpus target ranked first, yielding Recall@5/10 1.00 and MRR 1.0000 in all seven categories. `venv\Scripts\python.exe -m unittest tests.rag_eval.test_minilm_vector_baseline.MiniLMVectorRetrievalBaselineTests.test_all_minilm_identifier_retrieval_baseline -v` passed in 24.875 seconds: near-duplicate number lookup ranked third (MRR 0.3333), while the auxiliary filename control ranked ninth (Recall@5 0.00, Recall@10 1.00, MRR 0.1111); the other six identifier forms ranked first.
- Known limitations: the new category corpus is small and synthetic, so its all-rank-one result is a controlled capability check, not a production accuracy claim. The stress corpus is likewise synthetic but intentionally isolates identifier confusion; use it with real authorized fixture coverage before selecting a replacement model.

## 2026-08-10 - Compact local embedding alternatives benchmark

- Date: 2026-08-10.
- Task name: Benchmark compact local embedding alternatives after dense-only limitations.
- Affected files: `backend/scripts/benchmark_local_embeddings.py`, `backend/tests/test_embedding_alternative_benchmark.py`, `backend/tests/rag_eval/LOCAL_EMBEDDING_ALTERNATIVES.md`, and `changes/changes_backend`.
- What changed: added a local-only benchmark for the production MiniLM model plus BGE-small and E5-small. It uses a synthetic 72-passage near-duplicate corpus, reports Recall@5/10, MRR, embedding dimension, float32 dense-index estimate, warm query/corpus encoding time, estimated reindex time per 1,000 chunks, and Windows working-set delta. Candidate scope is fixed to compact local 384-dimension models; no production configuration, index, API, or documents changed.
- Why: Task 34 showed dense-only ranking weakness for identifiers, so alternatives needed measured comparison before any migration decision.
- Validation performed: `venv\Scripts\python.exe -m scripts.benchmark_local_embeddings --allow-download` completed in 118.6 seconds after downloading the two compact models, followed by a cache-only repeat in 26.7 seconds and a final Windows-RSS-corrected repeat in 14.5 seconds. Final metrics: MiniLM Recall@5/10 1.00/1.00, MRR 0.8167; BGE-small 1.00/1.00, 0.9000; E5-small 1.00/1.00, 0.9000. `venv\Scripts\python.exe -m unittest tests.test_embedding_alternative_benchmark -v` passed 2 tests. Both alternatives kept 384 dimensions and 1.46 MiB estimated dense vectors per 1,000 chunks; BGE had 16.72 ms median query encoding and E5 18.98 ms versus MiniLM 9.25 ms. `venv\Scripts\python.exe -m compileall scripts/benchmark_local_embeddings.py tests/test_embedding_alternative_benchmark.py` passed.
- Known limitations: model artifacts were downloaded once from Hugging Face for this authorized local benchmark and remain in the user cache; Windows cache symlink support is disabled, so local cache storage can be less efficient. Working-set deltas are process-incremental rather than isolated peak memory. This small synthetic corpus cannot establish production generalization; retain MiniLM plus hybrid retrieval unless a larger authorized fixture benchmark demonstrates a material Recall@5 improvement. BGE and E5 model cards list MIT licenses; E5 requires query/passages prefixes for retrieval.

## 2026-08-10 - Conservative retrieval-query normalization

- Date: 2026-08-10.
- Task name: Normalize presentation-only query variation before retrieval.
- Affected files: `backend/app/utils/query_normalization.py`, `backend/app/services/vector_search.py`, `backend/tests/test_query_normalization.py`, and `changes/changes_backend`.
- What changed: added a small shared normalizer used only by vector and keyword retrieval. It applies Unicode NFKC normalization, collapses whitespace, maps typographic dashes/quotes to their ASCII equivalents, converts numeric `percent`/`per cent` forms to `%`, and removes commas only from strict grouped numeric literals. It deliberately retains case, filename punctuation, identifiers, entity spelling, and factual wording. The original chat question remains unchanged for routing, follow-up, and answer generation.
- Why: visually equivalent query forms such as non-breaking spaces, Unicode dashes, `50 percent`, and `1,250` should retrieve consistently without turning query cleanup into semantic rewriting.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_query_normalization tests.test_keyword_search tests.test_source_selection_gate -v` passed all 27 tests in 5.775 seconds. New tests verify normalized whitespace/Unicode punctuation/percent/numeric formats, preservation of filenames and identifiers, and identical normalized handoff to keyword and vector retrieval. `venv\Scripts\python.exe -m compileall app/utils/query_normalization.py app/services/vector_search.py tests/test_query_normalization.py` passed; `git diff --check` passed with existing CRLF normalization warnings only.
- Known limitations: normalization intentionally does not interpret locale-specific decimal conventions, expand abbreviations, convert currency units, resolve aliases, or rewrite text words such as `one thousand`; those operations could alter factual meaning or identifiers and remain outside this safe pre-retrieval layer.

## 2026-08-10 - Query-phrasing rewrite assessment

- Date: 2026-08-10.
- Task name: Assess whether evaluation failures justify query rewriting.
- Affected files: `backend/tests/rag_eval/test_manual_query_phrasing_assessment.py`, `backend/tests/rag_eval/QUERY_PHRASING_ASSESSMENT.md`, and `changes/changes_backend`.
- What changed: added an evaluation-only, local fixture comparison between original queries and manually clarified equivalents. It covers natural-language spreadsheet counts/aggregations/source selection, verbose requests, and a context-dependent follow-up. No production retrieval or generation code was changed and no LLM rewrite path was added.
- Why: distinguish genuine retrieval failures caused by wording from answer-planning defects and avoid unsafe rewriting that could invent follow-up referents or bypass the no-global-search safeguard.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.rag_eval.test_manual_query_phrasing_assessment -v` passed all 3 tests in 18.700 seconds: all natural-language and verbose pairs selected identical structured evidence; the explicit follow-up form used structured retrieval only after removing prior context, while `show those` stayed on the bounded follow-up path with the same source. `venv\Scripts\python.exe -m unittest tests.rag_eval.test_minilm_vector_baseline.MiniLMVectorRetrievalBaselineTests.test_all_minilm_category_benchmark -v` passed in 14.090 seconds, with abbreviation probes Recall@5/10 1.00 and MRR 1.0000. The complete regression baseline ran 28 tests in 47.440 seconds: all 14 retrieval checks passed; 13 of 14 answer checks passed; `follow_up_show_those` still returned price values rather than expected equipment names despite correct route/source.
- Known limitations: comparisons use small synthetic local fixtures and cannot prove performance on every future phrasing variant. The remaining `show those` issue is a follow-up result-plan/answer-formatting defect, not evidence that a retrieval-query rewrite would be safe or effective.

## 2026-08-10 - Controlled query rewriting decision

- Date: 2026-08-10.
- Task name: Conditionally implement controlled query rewriting.
- Affected files: `changes/changes_backend`.
- What changed: no implementation was made because the prerequisite evaluation did not demonstrate a meaningful retrieval benefit. Existing deterministic retrieval normalization and bounded follow-up handling remain unchanged.
- Why: Task 37 found that natural-language spreadsheet and verbose variants selected the same structured evidence, abbreviation probes already ranked first, and the remaining `show those` failure is answer formatting after correct retrieval. Adding a rewrite stage would add risk of changing intent or inventing a follow-up referent without measured value.
- Validation performed: relied on the immediately preceding local evaluation: the manual phrasing assessment passed 3/3 tests; MiniLM abbreviation probes reported Recall@5/10 1.00 and MRR 1.0000; the known regression suite had all 14 retrieval checks passing, with only the pre-existing follow-up answer-formatting failure.
- Known limitations: this is not a claim that rewriting can never help. Reconsider only if new authorized evaluation cases show a repeatable, semantically equivalent manual rewrite improves retrieval without injecting facts, changing identifiers/numbers/filenames/operators, or bypassing context safeguards.

## 2026-08-10 - Complete retrieval evaluation result

- Date: 2026-08-10.
- Task name: Run complete retrieval evaluation suite.
- Affected files: `changes/changes_backend`.
- What changed: no code or test case changed. Ran the existing local retrieval evaluation modules and recorded the current measurements below.
- Why: establish a current evidence baseline for retrieval quality, source routing, and structured workbook selection without changing runtime behavior.
- Validation performed: the retrieval/framework/metrics/manual-phrasing group passed all 31 tests in 44.158 seconds. The five MiniLM retrieval evaluations passed when run separately: clean-category benchmark (14.554 seconds), identifier baseline (20.984 seconds), ranking-quality report (22.443 seconds), score-distribution report (21.006 seconds), and fusion/rollback configuration check (included in the clean-category command). The all-at-once `unittest discover -s tests\\rag_eval -v` attempt exceeded the shell's 60-second command limit after reporting the known final-answer failure; all retrieval-only modules then completed successfully. On the nine-query near-duplicate-plus-broad corpus, vector-only baseline versus current hybrid was: Hit Rate@5 0.8889 -> 1.0000; Recall@5 0.8889 -> 1.0000; Recall@10 1.0000 -> 1.0000; Precision@5 0.1778 -> 0.2000; MRR 0.8272 -> 0.9111. The clean vector-only category corpus has two queries each for semantic paraphrase, names, numbers, workbook labels, document headings, abbreviations, and cross-domain terminology; every query ranked first (Hit Rate@5 1.00, Recall@5/10 1.00, Precision@5 0.20, MRR 1.0000). Routing accuracy was 14/14 (1.00): 8/8 structured workbook contracts, 4/4 bounded follow-up contracts, and 2/2 unavailable/clarification contracts. Structured-query accuracy was 8/8 (1.00), including selected source and relevant worksheet provenance where specified. Per identifier query type, hybrid improved filename from rank 9 to rank 5 (Recall@5 0.00 -> 1.00, MRR 0.1111 -> 0.2000) and exact number from rank 3 to rank 1 (MRR 0.3333 -> 1.0000); all other tested identifier types ranked first in both modes.
- Known limitations: all corpora are isolated and synthetic, so these are regression signals rather than production traffic measurements. There are no remaining retrieval, routing, or structured-query contract failures in this suite. One separate final-answer failure remains: valid follow-up `show those` returns the prior price values rather than the expected equipment names; it has correct routing and source selection and is not counted as retrieval failure.

## 2026-08-10 - Final answer-generation evaluation result

- Date: 2026-08-10.
- Task name: Evaluate final answer generation using the retrieval baseline.
- Affected files: `changes/changes_backend`.
- What changed: no code, prompt, model, or test case changed. Evaluated the existing final-answer contracts together with current retrieval, final-context, grounding, unavailable-answer, citation, and prompt-injection guard tests.
- Why: determine whether the remaining quality issue is generation before considering any model tuning.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.rag_eval.test_known_failure_regressions.KnownFailureAnswerBaselineTests -v` ran 14 deterministic answer contracts in 25.816 seconds: answer-contract correctness 13/14 (92.9%), factual completeness 10/11 cases with required facts (90.9%), grounded/unavailable state correctness 14/14 (100%), and unavailable-answer correctness 2/2 (100%). The single incomplete answer, `show those`, returns only source-grounded values (`125000`, `75000`, `200000`) instead of the requested matching record names; it does not invent a fact. Source/routing provenance was 14/14 in the immediately preceding retrieval evaluation, and `venv\Scripts\python.exe -m unittest tests.test_chat tests.test_final_context_selection tests.test_rag_prompt_injection -v` passed all 28 tests in 0.078 seconds, covering citation-required grounded replies, no citations for unavailable/irrelevant/uncitable evidence, evidence-aware context diversity/deduplication/neighbors/token budget, and untrusted-document prompt boundaries. Contract-level citation correctness is therefore 14/14 expected source selections with no detected citation-validation failure; observed hallucination rate is 0/14 (0%) under the narrow definition of a claim not supported by the controlled source evidence.
- Known limitations: these are deterministic, synthetic-fixture contracts; they do not score open-ended LLM prose quality or provide a statistically valid production hallucination rate. Failure classification: retrieval 0; final-context selection 0; generation/model 0; citation validation 0; follow-up result-plan/context representation 1. The failure occurs before LLM generation and after correct retrieval, so model tuning is not justified. A future targeted fix should retain the filtered row identities/columns needed for a list-style follow-up rather than replaying only scalar values.

## 2026-08-10 - Focused RAG security regression review

- Date: 2026-08-10.
- Task name: Review RAG security boundaries after retrieval changes.
- Affected files: `backend/tests/test_rag_hybrid_security.py` and `changes/changes_backend`.
- What changed: added two isolated hybrid-search regressions that inject private, cross-organization, soft-deleted, and non-current vector-provider chunk IDs. They prove the hybrid path's authoritative SQLite recheck discards them before rank fusion can return a result. No production security behavior required a fix.
- Why: keyword search already had lifecycle/ACL coverage, while hybrid fusion needed explicit defense-in-depth coverage that an untrusted or stale vector-provider candidate cannot bypass the same authorization filter.
- Validation performed: `venv\Scripts\python.exe -m unittest tests.test_security_limits tests.test_keyword_search tests.test_rag_diagnostic_endpoint tests.test_rag_diagnostics tests.test_rag_prompt_injection tests.test_rag_hybrid_security -v` passed all 30 tests in 24.129 seconds. This covers tenant/owner/private ACL boundaries, deleted/current-version filtering, diagnostic authentication/disabled default/ACL response filtering/no content previews/no secret error disclosure, safe diagnostic schema, prompt-injection boundaries, and keyword plus hybrid access filters. `venv\Scripts\python.exe -m unittest tests.test_multitenant_architecture -v` passed all 15 tests in 33.406 seconds, covering organization isolation, ownership, sharing, version lifecycle, and soft deletion. `venv\Scripts\python.exe -m unittest tests.test_chat tests.test_final_context_selection -v` passed all 26 tests in 0.084 seconds, including no citations for unavailable, irrelevant, or uncitable evidence and bounded final context. `git diff --check -- backend/tests/test_rag_hybrid_security.py changes/changes_backend` passed with only existing CRLF conversion warnings.
- Known limitations: this is a focused application-layer regression review using synthetic data and injected provider identifiers; it is not a penetration test or production log-retention audit. No confirmed RAG security regression was found. The test runner emits only fixture-safe operational identifiers/statuses; production logging policy should continue to avoid query text, document content, secrets, authentication headers, and private filenames except where an authorized product response explicitly requires a filename.

## 2026-08-10 - Primary RAG pipeline latency benchmark

- Date: 2026-08-10.
- Task name: Benchmark the approved local RAG pipeline.
- Affected files: `changes/changes_backend`.
- What changed: no production or test code changed. Ran an ad-hoc local-only benchmark against a temporary database, temporary local-Qdrant directory, the configured all-MiniLM-L6-v2 model, and the existing 72-chunk synthetic identifier corpus. It instrumented the current hybrid path without sending documents or prompts to an external service.
- Why: locate material latency and memory costs without trading away the retrieval gains that hybrid fusion provides.
- Validation performed: current configuration was verified as `RAG_RETRIEVAL_MODE=hybrid`, `VECTOR_STORE=qdrant_local`, `QDRANT_MODE=local`, and no reranker. Median warm-stage timings over 10 retrievals were: direct query embedding 10.239 ms; direct keyword retrieval 5.699 ms; direct local-Qdrant vector retrieval 27.875 ms; integrated hybrid total 63.890 ms; integrated embedding 11.116 ms; integrated keyword 5.397 ms; integrated vector stage 32.762 ms; fusion plus authoritative SQLite recheck/result shaping 10.501 ms; final context selection over 50 runs 0.006 ms. The expected filename chunk remained rank 5 in the five-result hybrid response, preserving the existing retrieval result. No reranking stage is enabled, so reranking latency is 0/not applicable. LLM response latency was intentionally not measured because no external provider call was made. The local model benchmark measured 384 dimensions, 206.90 ms cache-resident load, 9.12 ms median warm query embedding, and +20.12 MiB Windows working-set delta. Compared with the prior local baseline of 9.25 ms query embedding and +19.15 MiB RSS, current embedding latency is effectively unchanged (-0.13 ms) and memory is +0.97 MiB; no earlier whole-pipeline timing exists for a like-for-like comparison. `venv\Scripts\python.exe -m unittest tests.test_embedding_alternative_benchmark tests.rag_eval.test_minilm_vector_baseline.MiniLMVectorRetrievalBaselineTests.test_fusion_configuration_controls_candidates_and_vector_rollback -v` passed all 3 tests in 11.778 seconds.
- Known limitations: this is a warm, synthetic 72-chunk workload on a temporary local-Qdrant directory, not a production-size corpus or end-to-end HTTP benchmark. Cold model startup, LLM network/provider latency, and external queueing are deliberately excluded. The expensive retrieval stages are local-Qdrant search and embedding, not context construction or keyword scoring. Recommended follow-up, separately from any behavior change: collect the same stage timings under an authorized representative corpus; profile Qdrant filtering/index behavior before reducing candidate limits; and retain hybrid fusion unless a recall/MRR regression suite shows that a candidate reduction is harmless. Do not optimize away ACL rechecks or switch models solely for the small observed timing difference.

## 2026-08-10 - Complete supported validation

- Date: 2026-08-10.
- Task name: Run complete supported backend, RAG, security, workbook, vector, and frontend validation.
- Affected files: `backend/tests/test_chat_context.py` and `changes/changes_backend`.
- What changed: no production code changed. Corrected one RAG test fixture so its mocked prior context contains the same validated result-plan and source-sheet provenance required by the production follow-up safety gate.
- Why: the fixture had become incomplete after the approved provenance hardening and therefore represented a context that production would correctly reject; the correction preserves the security constraint while exercising a valid recent grounded context.
- Validation performed: Python syntax check `venv\Scripts\python.exe -m compileall app scripts tests` passed. Backend unit tests were run in supported bounded groups: ingestion/parser/reindex 76 passed; workbook/structured retrieval 49 passed; embeddings/vector/Qdrant/normalization/repair 32 passed; chat/follow-up/source selection/diagnostics/security/final-context 87 passed; RAG evaluation framework/metrics/manual phrasing/regressions 44 passed and 1 known baseline failed; MiniLM vector evaluations 5 passed; multitenant/lifecycle 15 passed. Total backend result: 309 tests run, 308 passed, 1 failed, 0 skipped. `npm run build` passed after TypeScript compilation and Vite production bundling; no frontend test script is configured in `frontend/package.json`. `git diff --check` passed with only existing CRLF conversion warnings.
- Known limitations: the sole unresolved failure is the pre-existing `tests.rag_eval.test_known_failure_regressions.KnownFailureAnswerBaselineTests.test_answer__follow_up_show_those`: it retrieves the right CSV source but lists price values instead of the expected equipment names. It existed before this final validation and remains intentionally unfixed because it is a separate deterministic follow-up result-plan issue, not caused by the validation fixture correction. The frontend build reports Vite's non-fatal 500 kB chunk-size warning; no frontend behavior failure was found.

## 2026-08-10 - Final RAG architecture documentation

- Date: 2026-08-10.
- Task name: Document the implemented end-to-end RAG architecture.
- Affected files: `docs/RAG_ARCHITECTURE.md` and `changes/changes_backend`.
- What changed: added a documentation-only request-flow reference covering follow-ups, structured routing, local vector retrieval, local keyword retrieval, deterministic rank fusion, context selection, answerability, generation, citation validation, diagnostics, configuration, security controls, fallbacks, and regression coverage. It explicitly records that reranking and LLM query rewriting are not enabled.
- Why: provide an accurate technical handoff after the completed RAG work without implying unsupported capabilities or changing application behavior.
- Validation performed: documentation was cross-checked against `backend/app/config.py`, `.env.example`, `rag_service.py`, `chat_context.py`, `source_selection.py`, `vector_search.py`, `keyword_search.py`, `rag_diagnostics.py`, and the named regression suites. `git diff --check` is run as part of final validation.
- Known limitations: this is implementation documentation, not an API compatibility guarantee or production-scale performance claim. It retains the known list-style follow-up formatting limitation and deferred features listed in the document.

## 2026-08-10 - Follow-up result preservation and provenance validation

- Date: 2026-08-10.
- Task name: Resolve follow-up entity replay and modernize follow-up provenance fixtures.
- Affected files: `backend/app/services/workbook_analysis.py`, `backend/app/services/chat_context.py`, `backend/tests/test_chat_context.py`, `backend/tests/test_workbook_analysis_generic.py`, and `backend/tests/test_embeddings.py`.
- What changed: structured aggregate/count results now retain one explicitly referenced, non-numeric display column and its matching entity values for safe referential follow-ups. Follow-up replay honors that persisted display column. The valid chat-context fixture now contains document/version/source-sheet/result-plan/row provenance, and a negative mismatch case confirms invalid plans remain unavailable. The embedding cache test resets process-local state before each assertion so complete-suite ordering cannot bypass its concurrent-load assertion.
- Why: a numeric range question was planned as an aggregate over `Price`; its context therefore retained numeric operands and replayed them for `show those`, even though the question explicitly referenced the matching entity column. The old fixture omitted provenance required by the current validator. The full-suite embedding failure was test-state leakage, not a production model-loading defect.
- Validation performed: focused follow-up/workbook/evaluation tests passed 101/101; focused Qdrant/vector/repair tests passed 22/22. Full supported backend discovery passed 311/311 in 437.271 seconds. Python compile/import, frontend production build, and `git diff --check` passed; the frontend build retained only its existing non-blocking chunk-size warning. Read-only consistency confirmed 95 active SQLite chunks, 97 active Qdrant points, zero missing/duplicate/wrong-version/deleted-document vectors, and two orphan point IDs. Their payload metadata maps to chunk/document/version `1/1/1` and `2/2/2`, but neither has a SQLite chunk record.
- Known limitations: the existing repair command safely reindexes current chunks but does not delete arbitrary orphan point IDs. No Qdrant data was modified because the two points require a separately reviewed, targeted orphan-purge capability (or an operator-approved manual procedure); `changes_db` was not updated because no database or vector data was repaired.

## 2026-08-10 - Safe confirmed-orphan Qdrant repair

- Date: 2026-08-10.
- Task name: Extend vector consistency repair for confirmed orphan Qdrant points.
- Affected files: backend/app/services/vector_store.py, backend/scripts/check_vector_consistency.py, backend/scripts/repair_vector_consistency.py, backend/tests/test_vector_consistency.py, backend/tests/test_repair_vector_consistency.py, backend/tests/test_qdrant_vector_store.py, changes/changes_backend, and changes/changes_db.
- What changed: added a provider-level explicit-point deletion primitive and a --repair-orphans repair mode. It is dry-run by default and requires both --apply and --confirm-repair to write. Each candidate must be an active checker orphan with no same-organization SQLite chunk, no active current-version equivalent, and no queued/processing/retry ingestion job. Reports include only IDs and lifecycle metadata plus the proposed action.
- Why: legacy/default-organization Qdrant payloads for point IDs 5e6a91d4-dc43-539e-bd0f-1d275db56d2b and 64edf488-6065-5ec8-9292-f84d31b3bc4c remained active after their corresponding records were soft-deleted under a different organization and reindexed with replacement IDs. They could not be safely removed by the prior reindex-only repair command.
- Validation performed: before repair, consistency reported 95 active SQLite chunks, 97 active vectors, and exactly the two named orphan IDs. The --repair-orphans dry-run selected exactly those IDs without writes. The explicitly confirmed command deleted exactly those IDs with no reindex. The post-repair checker reports 95 SQLite chunks, 95 active vectors, zero orphan/missing/duplicate/wrong-version/deleted-document discrepancies. Focused vector/Qdrant/repair tests passed 21/21; compileall passed.
- Known limitations: physical deletion is intentionally available only to the operator-only repair command, never to retrieval or startup paths. The repair makes no inference from document text and will refuse candidates with a same-organization chunk, an active current equivalent, or active ingestion work.

## 2026-08-11 - Phase 2 generic RAG failure fixes

- Date: 2026-08-11.
- Task name: Implement confirmed generic fixes for mixed evidence, embedded OCR extraction, and elliptical follow-ups.
- Affected files: `backend/app/config.py`, `backend/.env.example`, `backend/app/services/rag_service.py`, `backend/app/services/source_extraction.py`, `backend/app/services/chat_context.py`, `backend/tests/test_final_context_selection.py`, `backend/tests/test_document_parsers.py`, `backend/tests/test_chat_context.py`, and `changes/changes_backend`.
- What changed: final context selection now keeps the primary evidence and may add bounded, relevant complementary evidence from a different family (table, prose, or image OCR) only from the same selected document/version. DOCX and PDF parsing now perform best-effort bounded OCR on internal embedded images using the existing image extraction abstraction; scanned PDFs with page images can produce page-provenance OCR chunks. Metric-only elliptical follow-ups such as `current rate?` now require compatible recent structured context and matching row headers before resolving; unresolved elliptical follow-ups return the standard unavailable answer instead of falling through to global retrieval.
- Why: Phase 1 confirmed three generic root causes: non-complex final context often carried only one chunk, embedded DOCX/PDF images were not OCRed, and metric-only continuations were not classified with context before retrieval.
- Configuration: added `RAG_COMPLEMENTARY_CONTEXT_LIMIT`, `RAG_COMPLEMENTARY_MIN_SCORE`, `EMBEDDED_OCR_MAX_IMAGES_PER_DOCUMENT`, `EMBEDDED_OCR_MAX_IMAGES_PER_PAGE`, `EMBEDDED_OCR_MAX_PIXELS`, and `EMBEDDED_OCR_MAX_DECODED_BYTES` with safe bounded defaults. Existing OCR timeout remains in the image OCR layer.
- Security/resource controls: OCR uses internal DOCX media parts and PDF page images only, does not follow external relationships, validates image bytes through Pillow, enforces decoded-byte and pixel limits, uses temporary directories with cleanup, skips malformed/oversized failed images without logging extracted content, and preserves existing ingestion, ACL, current-version, and grounding checks.
- Database and migration: no schema change and no migration required. Existing uploaded DOCX/PDF files need reprocess/reupload to gain new embedded OCR chunks.
- Validation performed: focused Phase 2 tests passed with `venv\Scripts\python.exe -m unittest tests.test_final_context_selection tests.test_chat_context tests.test_document_parsers -v` (54 tests). A full discovery run initially exposed that the new elliptical follow-up detector was too broad and intercepted fresh questions such as revenue/career-goal lookups; the detector was tightened to require short context-modifier language before using prior chat scope. The exact impacted regression cases then passed with `venv\Scripts\python.exe -m unittest tests.test_chat.RagRetrievalPolicyTests.test_metric_question_uses_report_pdf_evidence tests.test_chat.RagRetrievalPolicyTests.test_structured_lookup_uses_selected_source_without_llm tests.test_workbook_domain_neutral.WorkbookDomainNeutralTests.test_follow_up_uses_latest_grounded_context_within_same_chat tests.test_workbook_domain_neutral.WorkbookDomainNeutralTests.test_person_sheet_career_goal_lookup_handles_mixed_layouts tests.test_workbook_domain_neutral.WorkbookDomainNeutralTests.test_relevant_pdf_style_retrieval_wins_over_unrelated_workbook tests.test_rag_prompt_injection.RagPromptInjectionTests.test_embedded_commands_remain_data_and_cannot_remove_citations tests.test_qdrant_integration.QdrantIntegrationTests.test_qdrant_search_failure_returns_safe_error -v` (7 tests). Final full backend discovery passed with `venv\Scripts\python.exe -m unittest discover -s tests -v` (332 tests). Python compilation passed with `venv\Scripts\python.exe -m compileall app`, and `git diff --check` passed with only existing LF-to-CRLF warnings.
- Remaining limitations: PDF support uses pypdf-exposed page images rather than a full page renderer because no safe renderer dependency is currently present. If a scanned PDF stores content in a form not exposed through pypdf images, it may still be unavailable until a bounded renderer is added. OCR quality still depends on the configured local OCR/vision availability.

## 2026-08-14 - OCR failure root-cause investigation

- Date: 2026-08-14.
- Task name: Investigate image OCR and image-only DOCX OCR failures without changing application code.
- Affected files: `changes/changes_backend` only for this report. Temporary reproduction fixtures were generated under `changes/ocr_investigation_fixtures` for local evidence; no backend, frontend, database schema, prompt, dependency, or configuration code was changed.
- Chronology:
  1. Inspected current branch/status and the RAG Project engineering-loop instructions. Worktree was on `codex/fix-rag-followups-docx-ingestion`; no application-code edits were made.
  2. Located the canonical queued upload endpoint in `backend/app/routes/ingestion.py::queue_document_upload` and the legacy inline upload path in `backend/app/routes/upload.py::_process_document_upload`. The image endpoint `backend/app/routes/images.py::upload_image` delegates to the legacy inline upload path.
  3. Traced extraction through `backend/app/services/ingestion_jobs.py::_extract_bundle`, `backend/app/services/source_extraction.py::extract_source_chunks`, `backend/app/services/document_loader.py::extract_text`, and `backend/app/services/image_processor`.
  4. Verified runtime OCR configuration from the backend virtualenv. `pytesseract==0.3.13` imports successfully, but `tesseract --version` fails, `where.exe tesseract` finds no executable, `TESSERACT_CMD` is not set, `_configure_tesseract()` leaves `pytesseract.pytesseract.tesseract_cmd` as `tesseract`, `shutil.which('tesseract')` is false, and common Windows install paths did not contain `tesseract.exe`.
  5. Confirmed Tesseract language-pack discovery cannot run: `pytesseract.get_languages(config='')` raises `TesseractNotFoundError` because the executable is not found.
  6. Confirmed live API/worker launch commands through a read-only process query: API and worker are running from `backend\venv\Scripts\python.exe`, with Python child processes executing `uvicorn app.main:app --reload` and `app.worker`.
  7. Checked available local logs (`backend/api*.log`, `backend/worker*.log`); they had no useful OCR stack traces. Queried active SQLite `backend/data/rag_new.db` read-only and found recent failed jobs matching the observed failures: image uploads store `OCR is unavailable because the Tesseract service is not installed.`, while image-only DOCX uploads store `No readable content was found in the uploaded file.`
  8. Created four safe fixtures: a PNG with clear English text, a normal text DOCX, an image-only DOCX containing that PNG, and a mixed text-plus-image DOCX.
  9. Ran each fixture through signature validation, extractor selection, DOCX image introspection, direct OCR where applicable, `extract_source_chunks()`, and ingestion error classification.
  10. Ran a focused monkeypatch trace against the image-only DOCX. With fake OCR success, the DOCX path produced one `image_ocr` chunk. With fake OCR failure, the DOCX path called OCR once, swallowed the sentinel error, and raised `DocumentParseError("No readable content was found in the uploaded file.")` with no preserved cause.
- Reproduction results:
  - PNG image: MIME guess `image/png`; signature validation passed; extractor `image/OcrParser -> extract_image_text`; OCR attempted; failed at `extract_ocr()` with `OcrError("OCR is unavailable because the Tesseract service is not installed.")`; mapped ingestion status would be `failed`, code `document_parse_failed`, message `OCR is unavailable because the Tesseract service is not installed.`
  - Normal text DOCX: MIME guess `application/vnd.openxmlformats-officedocument.wordprocessingml.document`; signature validation passed; extractor `source_extraction._docx`; 1 paragraph, 0 embedded image parts; OCR not attempted; 1 prose chunk, extracted text length 53; extraction would complete.
  - Image-only DOCX: MIME guess `application/vnd.openxmlformats-officedocument.wordprocessingml.document`; signature validation passed; extractor `source_extraction._docx`; 1 paragraph, 1 embedded image part, 1 relationship location; OCR was invoked internally, but the real Tesseract failure was swallowed; final exception `DocumentParseError("No readable content was found in the uploaded file.")`; mapped ingestion status `failed`, code `document_parse_failed`, message `No readable content was found in the uploaded file.`
  - Mixed DOCX: MIME guess `application/vnd.openxmlformats-officedocument.wordprocessingml.document`; signature validation passed; extractor `source_extraction._docx`; 3 paragraphs, 1 embedded image part, 1 relationship location; native typed text produced 1 prose chunk; embedded OCR failure was swallowed as best-effort and did not fail the document; extraction would complete with typed text only and no `image_ocr` chunk.
- Exact root cause:
  - Configuration/dependency root cause: the backend cannot find a Tesseract executable. `backend/app/services/image_processor/ocr.py::_configure_tesseract` checks the current pytesseract command, `TESSERACT_CMD`, `%LOCALAPPDATA%\Programs\Tesseract-OCR\tesseract.exe`, and `%ProgramFiles%\Tesseract-OCR\tesseract.exe` (lines 27-45). None resolve in the current runtime, so `extract_ocr()` calls `pytesseract.image_to_string()` with command `tesseract` (lines 48-69), which raises `TesseractNotFoundError` and is mapped to `OcrError("OCR is unavailable because the Tesseract service is not installed.")` (lines 81-95).
  - Application-code root cause for DOCX masking: `backend/app/services/source_extraction.py::_docx` does invoke `_docx_image_chunks(document)` after prose/table extraction (lines 721-741). `_docx_image_chunks()` discovers internal image parts from `document.part.related_parts` and calls `_image_chunks_from_bytes()` (lines 230-258). `_image_chunks_from_bytes()` catches all exceptions from temporary-file OCR and returns `[]` (lines 124-131). For an image-only DOCX, no prose/table chunks exist; after the swallowed OCR error, `extract_source_chunks()` sees an empty chunk list and raises the generic no-readable-content error (lines 793-843).
- Whether image upload and image-only DOCX share the same failure: yes at the underlying dependency level. Both depend on the same local OCR stack and fail because Tesseract is unavailable to the backend process. They differ at error mapping: direct image upload preserves the OCR dependency message through `DocumentParseError`, while DOCX embedded-image OCR intentionally treats OCR as best-effort and converts dependency failure into empty chunks, causing a generic no-readable-content error for image-only DOCX.
- Dependency/configuration evidence:
  - `backend/requirements.txt` includes `pytesseract==0.3.13` and `python-docx==1.2.0`.
  - Backend venv import succeeded for `pytesseract`; Python executable was `backend\venv\Scripts\python.exe`.
  - `tesseract --version` failed with command not recognized.
  - `where.exe tesseract` found no executable.
  - `TESSERACT_CMD` was not set in effective backend settings.
  - Configured pytesseract command after `_configure_tesseract()` remained `tesseract`; `shutil.which()` returned no path; `os.path.isfile("tesseract")` was false.
  - No `tesseract.exe` was found in the common Windows install paths checked.
  - Language packs could not be listed because the executable was unavailable.
- OCR exception swallowing: confirmed. `source_extraction._image_chunks_from_bytes()` catches all exceptions around `extract_image_text(path)` and returns `[]`. The monkeypatch trace proved the DOCX image-only path calls OCR once; when OCR raises `RuntimeError("sentinel OCR dependency failure")`, the final exception is `DocumentParseError("No readable content was found in the uploaded file.")` with `__cause__ is None`, so the underlying OCR exception is discarded.
- Security and production safeguards observed:
  - MIME/format validation is partly signature-based rather than extension-only: `backend/app/utils/file_validation.py::validate_file_signature` checks magic bytes for images, PDFs, Office ZIP, and legacy Office files, and rejects executable masquerades (lines 27-38).
  - DOCX/OOXML archive safeguards exist: entry count, expanded size, compression ratio, traversal paths, and encrypted entries are checked in `_validate_office_archive()` (lines 41-64).
  - Embedded OCR limits exist in `backend/app/config.py`: max images per document 25, max images per page 5, max pixels 16,000,000, max decoded bytes 20,000,000 (lines 99-102).
  - Image OCR has a direct per-frame pixel limit 40,000,000 and pytesseract timeout 45 seconds in `backend/app/services/image_processor/ocr.py` (lines 10-11, 60-69).
  - Production parser isolation exists in `ingestion_jobs._extract_bundle()` using a spawned process and `parser_timeout_seconds`; development runs extraction inline (lines 103-156).
  - Temporary embedded-image files use `TemporaryDirectory`, which cleans up automatically (source_extraction lines 124-131).
  - Client messages are sanitized by classification/storage in `ingestion_jobs._classify_error()` and `_fail_or_retry()`; messages are capped to 500 chars before persistence (lines 170-202 and 502-555). However, direct image OCR currently exposes the operational fact that Tesseract is not installed; that is useful locally but may be too specific for a hardened production client message.
  - Server-side logging captures failure stage and exception type without document contents in `ingestion_jobs.process_job()` (lines 1262-1278). The current DOCX swallow point logs no embedded OCR failure detail, so image-only DOCX loses the actionable server-side reason.
- Generic production-safe fix recommendation:
  - Install Tesseract on the host or container image and either add it to PATH for the backend/worker service account or set `TESSERACT_CMD` to a non-user-specific deployment path. Validate at startup/readiness that OCR is available when image OCR is enabled, including version and required languages, while not exposing internal paths to clients.
  - Change embedded OCR handling so dependency/configuration failures are distinguishable from per-image unreadable/oversized/malformed best-effort failures. For documents with no native text and at least one embedded image, preserve and surface a sanitized OCR-unavailable error instead of converting it to no-readable-content. For mixed documents, continue indexing native text but log a structured server-side OCR warning without document content.
  - Avoid hard-coded user-specific Windows paths. Prefer service configuration (`TESSERACT_CMD`) and deployment documentation.
  - Keep client errors generic in production, for example `OCR is unavailable for image-based documents. Please contact support or retry after OCR is configured.`, while logging server-side `TesseractNotFoundError`/configured-command diagnostics without secrets or raw document content.
- Tests that should be added:
  - Unit test: image upload extraction maps `pytesseract.TesseractNotFoundError` to a sanitized OCR-unavailable parse error.
  - Unit test: DOCX with only embedded image and mocked OCR success produces an `image_ocr` chunk with Word provenance.
  - Unit test: DOCX with only embedded image and mocked OCR dependency failure preserves an OCR-unavailable failure rather than generic no-readable-content.
  - Unit test: mixed DOCX with native text plus OCR failure still extracts native prose and records/logs an OCR warning without failing the document.
  - Unit test: embedded image count, decoded byte, pixel, and timeout limits are enforced without leaking paths/content.
  - Integration test: queued ingestion job for image-only DOCX stores a stable `document_parse_failed` code with a useful sanitized message.
  - Runtime/preflight test or health/readiness check: configured `TESSERACT_CMD` exists, is executable by the backend service account, returns a version, and lists required language packs.
- Final verdict: both. The immediate root cause is a configuration/deployment issue: Tesseract executable is not installed or not visible to PATH/configuration for the backend/worker process. There is also an application-code issue: embedded DOCX OCR catches and discards all OCR exceptions, so image-only DOCX reports a misleading no-readable-content error instead of preserving the real OCR dependency failure. OCR investigation: ROOT CAUSE CONFIRMED

## 2026-08-14 - Reliable OCR configuration and error propagation

- Date: 2026-08-14.
- Time: 10:11:53 +05:30.
- Task name: Implement reliable OCR for direct images and image-based documents.
- Affected files: `backend/app/config.py`, `backend/.env.example`, `backend/app/services/image_processor/ocr.py`, `backend/app/services/image_processor/image_parser.py`, `backend/app/services/document_loader.py`, `backend/app/services/source_extraction.py`, `backend/app/services/ingestion_jobs.py`, `backend/app/routes/health.py`, `backend/app/main.py`, `backend/README.md`, `backend/tests/test_document_parsers.py`, `backend/tests/test_multitenant_architecture.py`, `backend/tests/test_qdrant_integration.py`, and `changes/changes_backend`.
- What changed:
  - Added explicit OCR configuration fields `TESSERACT_CMD` and `OCR_REQUIRED_LANGUAGES`.
  - Added safe OCR readiness validation that checks the Python OCR package, Tesseract executable, version command, and required languages without exposing executable paths in health responses.
  - Added production startup fail-fast behavior when OCR is unavailable.
  - Replaced raw/legacy OCR messages with sanitized typed failures: `ocr_unavailable`, `ocr_processing_failed`, `ocr_timeout`, and `ocr_no_text`.
  - Preserved typed OCR infrastructure failures through direct image parsing, embedded DOCX/PDF extraction, and ingestion-job classification.
  - Kept corrupt or oversized embedded images best-effort so readable native DOCX/PDF text still indexes, while OCR dependency, execution, and timeout failures now fail OCR-dependent documents accurately.
  - Added `content_type: image_ocr` metadata to direct-image chunks in the queued source-extraction path.
  - Updated health responses to include safe `ocr.status` and documented deployment/configuration requirements.
- Why this approach:
  - The smallest root-cause fix was to make OCR dependency state explicit and typed, then preserve those typed failures across embedded extraction instead of returning an empty result. This preserves old successful text/table behavior and avoids hard-coded user-specific executable paths.
- Tests run:
  - `.\venv\Scripts\python.exe -m compileall app` passed.
  - `.\venv\Scripts\python.exe -m unittest tests.test_document_parsers -v` passed 29/29.
  - `.\venv\Scripts\python.exe -m unittest tests.test_image_upload tests.test_security_limits tests.test_multitenant_architecture -v` passed 26/26.
  - `.\venv\Scripts\python.exe -m unittest tests.test_qdrant_integration -v` passed 12/12.
  - `.\venv\Scripts\python.exe -m unittest discover -s tests -v` passed 350/350 in 192.804 seconds.
  - `git diff --check` passed with only existing LF-to-CRLF working-copy warnings.
- Manual verification:
  - Current local OCR health returns `{'status': 'unavailable'}` because Tesseract is still not installed/configured on this machine.
  - Direct PNG fixture now fails with code `ocr_unavailable` and message `OCR is currently unavailable. Please contact the administrator or try again later.`
  - Image-only DOCX fixture now fails with the same `ocr_unavailable` message instead of `No readable content was found in the uploaded file.`
  - With mocked OCR success, the PNG fixture produced one `image` chunk with `content_type=image_ocr`; the image-only DOCX fixture produced one `word` chunk with `content_type=image_ocr`.
- Security review:
  - Client-facing errors are sanitized and do not expose executable paths, stack traces, document contents, or secrets.
  - Server logs record OCR readiness/error type without extracted document text.
  - Existing file signature checks, Office archive traversal/decompression limits, parser timeout, embedded-image byte/pixel/image-count limits, temporary-directory cleanup, tenant/owner ACLs, and retry/idempotency behavior are preserved.
  - Failed OCR jobs remain failed and do not create successful chunks/vectors; retry can complete after OCR becomes available.
- API documentation:
  - `GET /health` now includes `ocr: {"status": "ready"}` or `ocr: {"status": "unavailable"}`.
  - `GET /health/ready` includes the same safe OCR status alongside database, vector-store, and embedding readiness.
  - Upload/ingestion failures for OCR dependency, execution, timeout, and no-text image cases use stable sanitized messages and stored error codes.
- Database documentation:
  - No schema change and no migration required. Existing failed OCR jobs can be retried after Tesseract is installed/configured. Existing successfully indexed documents are unchanged; OCR-dependent documents that previously failed need retry/reupload after configuration is corrected.
- Frontend design documentation:
  - No frontend files changed. Existing upload status surfaces the backend's sanitized job error message; no UI layout, typography, color, accessibility, or interaction behavior changed.
- Commit message:
  - `fix: preserve OCR dependency failures`
  - Body:
    - add explicit Tesseract configuration and safe OCR health status
    - propagate embedded-image OCR infrastructure failures through ingestion
    - add regression coverage for image and DOCX OCR error mapping, retry integrity, and health behavior
- Remaining risks/prerequisites:
  - Local and production environments must install Tesseract 5.x and the configured language packs, or set `TESSERACT_CMD` to a valid deployment path. This code verifies readiness but does not install the external executable.
  - Optional vision analysis remains an enhancement and does not replace required local OCR for OCR-dependent ingestion.
- Final status: PASS WITH WARNING. Application behavior is fixed and all backend tests pass, but this machine still needs Tesseract installed/configured before real non-mocked OCR uploads can succeed.
